# M129 Independent Review: Coherent Veronese Phase-Chart Closure

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Resident coherent phase-carrier restoration:
`EXACT_ALGEBRAIC_RESTORATION`

Transient projection, commitment, compiler, full-occupation oracle, and
classical-baseline buffers: `NO_RESTORATION_CLAIM`

The verified scientific source is commit
`f787e13100715219b8d39bd113f5aaade3695013`. M129 is a nonlinear,
program-restricted successor to M128. It does not weaken or reinterpret the
M128 uniform-linear irreducibility result.

## Verified mechanism

The accepted carrier stores the 17 exact coefficients of a rank-one coherent
form

```text
(sum_j v_j x_j)^k.
```

For the fixed public `p1,...,p4` character and bidirectional adjacent-mode
shear words, character application is a diagonal update of `v_j`, and a shear
is the exact one-particle substitution `v_row += zeta17^e v_pivot`. The
ordinary-monomial occupation boundary is reconstructed as
`k*v0^(k-1)*v1`. The inverse subtracts the same shear coefficient; it does not
use its reciprocal.

Exact `Q(zeta17)` transactions pass at `k=4,8,16,32`; structural `F103/F137`
transactions pass at `k=4,8,16,32,64,128`. The resident carrier remains 17
field cells and 762 counted exact payload bits across the exact cases while
the implicit occupation dimensions grow through
`4,845`, `735,471`, `601,080,390`, and `2,254,848,913,647`. The counted final
boundary payload grows from 197 to 1,545 bits and is not hidden from resource
accounting.

Only the final occupation scalar is projected. The forward chart is committed
one field record at a time and is not returned. Actual reverse execution
restores exact zero on the same backing, advances the restoration generation,
and supports an unrelated `k=16` reuse transaction after `k=8`; fresh and
restored boundaries and complete reported resource signatures agree without
snapshot reload or inverse history.

## Independent reconstruction and controls

The independent oracle separately reconstructs the public compiler,
17-coordinate chart recurrence, ordinary multinomial convention, inverse,
projection, streamed commitment, and compiled two-scalar classical baseline.
It expands the complete `H(4)=4,845` occupation recurrence in `Q(zeta17)`,
`F103`, and `F137`, and independently checks every one of the 40 primitives
and its inverse in both finite fields.

The actual M128 `k=2` grid transactions are reconstructed through separate
M127/M128 reference recurrences. Their de-multinomial binary slices have
nonzero rank-one catalecticant minors 58 and 46 for PRIMARY and REUSE, so those
grid-orbit injections leave the rank-one chart. The independent prior carrier
is required to restore. This is a bounded counterexample at `k=2`, not a
claim about every declared degree.

Wrong, missing, reciprocal, and reordered inverses; wrong ownership;
premature projection; null carrier; invalid degree; rank-two descriptors;
nonadjacent and grid-orbit shears; semantic mutations; and a noncoherent
rank-two witness are rejected or detected. No accepted-path occupation
vector, occupation topology, character table, dense operator, coherent-term
expansion, intermediate amplitude payload, or relation/assignment table is
materialized.

## Matched baseline and claim ceiling

For descriptor-runtime execution, the matched compact classical method is the
identical 17-coordinate recurrence. For each sealed fixed word, the stronger
warm baseline compiles through 17 working cells, retains only `(v0,v1)`, and
evaluates the closed-form boundary with two retained field cells and four
named warm transients. All 16 baseline cases are independently reconstructed
and boundary matched. No computational advantage is claimed.

Python containers, native allocator and bigint internals, hashlib internal
state, bit-operation complexity, and whole-process peak are excluded where
declared. The strict ceiling is the rank-one coherent Veronese chart, the
fixed public `p1,...,p4` character and bidirectional adjacent-mode-shear words,
exact `Q(zeta17)` at `k=4..32`, dual-field structural parity at `k=4..128`,
and direct-process software execution.

M129 does not establish M127 grid closure, sums of coherent components,
arbitrary `H(k)` input closure, a general nonlinear quotient, CATVM custody, a
distinct phase resource, computational advantage, Small Wall crossing,
physical waveform execution, replacement of physical bits with pi, or
unbounded catalytic computation.

The next obstruction is that the rank-one chart excludes the actual grid
coupling and generic superpositions, while each sealed word has a stronger
two-scalar warm classical recurrence. The next experiment must test the
smallest exact bounded secant or Gaussian coherent chart admitting one shared
nonintegrable orbit coupling, measure rank growth, and retain the strongest
compact classical baseline without matching or assignment enumeration.
