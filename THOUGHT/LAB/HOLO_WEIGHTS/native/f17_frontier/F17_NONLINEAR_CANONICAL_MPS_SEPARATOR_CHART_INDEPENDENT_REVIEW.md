# M122 Independent Review: Nonlinear Canonical MPS Separator Chart

## Decision

```text
classification       INDEPENDENTLY_VERIFIED_STRICT_SCOPE
verification         INDEPENDENT_ORACLE_REEXECUTION
carrier restoration  EXACT_ALGEBRAIC_RESTORATION
transient buffers    NO_RESTORATION_CLAIM
hosted execution     NOT ESTABLISHED
```

No claim-blocking source defect remains in the bounded package.

The independent oracle imports neither production nor a phase/CATVM backend.
It reconstructs the public descriptors, dense row recurrence, exact boundary,
inverse, every separator-subset matricization rank, and bounded all-order
decision-diagram controls. Live production and oracle outputs match their
committed seals.

## Preserved strict findings

- Exact `Q(zeta17)` production/oracle boundary and natural-rank parity holds
  for both public descriptor families at `n=2,3,4`.
- Forward execution followed by the actual reverse schedule restores the
  canonical TT state exactly, increments the restoration generation once,
  clears the lease, retains the same carrier backing, and permits unrelated
  fresh/restored reuse without snapshot reload or retained inverse history.
- The exact `n=4` `GENERIC` discriminator has rank profile `[2,4,2]`, 40 raw
  TT core cells, 24 gauge dimensions, and 16 effective coordinates. Every
  separator-bit order has maximal ranks. Its exact fully reduced projective
  EVDD has 15 nonterminal nodes. Neither nonlinear chart is smaller than the
  16-coordinate or 15-node full representation on this fixture.
- Exact amplitude-bearing projectivization retains 15 ratios plus one scale
  at `n=4`; its ratio denominators reach 91 bits. It is a coordinate change,
  not separator compression.
- Dual-field production/oracle parity holds through `n=8`. `F103` supplies a
  full all-order modular rank certificate at every tested width. `F137` is
  degenerate for some `n=8` `GENERIC` subset ranks and is not represented as
  a second full certificate there.
- The direct zero-field planar-Ising/Pfaffian positive control is applicable.
  The one-site mutation and the `GENERIC` discriminator reject that direct
  route. Arbitrary basis changes and broader holographic reductions remain
  unexhausted.
- Production never materializes a `2^n` row vector. It reports saturated raw
  cores, effective chart coordinates, pivot metadata, exact resident payload,
  denominator/numerator height, operations, and scratch field cells.

## Corrections made during review

- A callable projection helper is present. The accepted transaction is now
  described only as having no intermediate projection call; static
  qualification verifies its sole projection lies between forward and inverse
  schedules.
- Decision-diagram counts are labelled quasi-reduced when redundant-test
  deletion is not proved. The exact `n=4` fully reduced 15-node result is
  retained because every proper-prefix residual class is full.
- Scratch coefficient payload is an estimate from resident-cell height, not a
  measured upper bound on RREF intermediates. Exact scratch-bit accounting is
  not claimed.
- The reordered-inverse control now moves one noncommuting vertical inverse
  ahead of the final row diagonal without duplicating either inverse.

## Claim ceiling

This is an exact `Q(zeta17)` two-family `n=2,3,4` nonlinear-chart diagnostic
plus dual-field structural execution through `n=8`. It does not reject all
nonlinear, program-dependent, restricted-signature, ADD/MTBDD, MPS,
matchgate, holographic, global, or boundary-specific methods.

The coefficient zero tests, inversions, RREF, pivot selection, and gauge
fixing are ordinary software operations, not resident phase primitives. The
package establishes no CATVM custody, distinct phase resource, computational
advantage, Small Wall crossing, physical waveform execution, physical bit
replacement, or unbounded computation.
