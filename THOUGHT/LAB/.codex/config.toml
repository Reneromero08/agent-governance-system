# Lab isolation is an instruction and Git-routing boundary, not a filesystem
# sandbox. Keep the user's normal Codex permissions when entering the Lab.

[shell_environment_policy.set]
PYTHONDONTWRITEBYTECODE = "1"
