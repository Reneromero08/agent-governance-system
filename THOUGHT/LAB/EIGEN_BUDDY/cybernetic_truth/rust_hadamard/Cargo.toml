[package]
name = "rust_hadamard"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_hadamard"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
ndarray = { version = "0.16", features = ["rayon"] }
rayon = "1.10"
