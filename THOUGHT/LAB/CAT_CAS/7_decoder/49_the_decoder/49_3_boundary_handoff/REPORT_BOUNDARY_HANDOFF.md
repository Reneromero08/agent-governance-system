# Exp 50.3 — Boundary Characterization + Handoffs

**Verdict:** `BOUNDARY_CHARACTERIZED_HANDOFFS_EMITTED`. Claim level **5** (a located boundary + two scoped questions — not an answer).

## Overview

Synthesizes Bricks 1+2 and emits two self-contained handoff artifacts **at runtime** from the empirically located collapse (no hand-authored values).

## The boundary, characterized

- **Extractive (Brick 1):** the holographic readout reads a global invariant no lookup-class decoder can (Cohen h > 2.4, p = 2e-4), surviving a statistics-matched wrong-answer control.
- **Bounded (Brick 2):** that extractive power decodes exactly the abelian / 1-D-character class and **collapses at the first non-abelian group, D_8** (d_max 1→2). Shelf D = 1.000, pole D = 0.110, Cohen d = 8.82, scale-independent. The cospectral anchor independently confirms the readout is spectrum-bounded.

So the decoder's reach is: **decodable ≈ abelian-HSP / Fourier-decodable; collapse at the abelian→non-abelian boundary.** This is the operating manual the lab was missing.

## Handoff A — `MYTHOS_SANDBOX.md`

Self-contained brief carrying the located wall (D_8), the sub→at→super-wall table, and the binary question: **is the collapse the known non-abelian-HSP / lattice barrier, or a reframe-able frontier the scalar readout simply hasn't crossed?** Includes a minimal reproducer bound to the SAME null harness (`decoder_lib.null_model` + `hsp_family`), so any proposed reframe is testable: it counts only if it lifts D at D_8 above the null with effect outside the null CI, without also lifting the label-shuffle null. This is the one surgical question reserved for the stronger model — built on a null that can kill a false reframe.

## Handoff B — `EXP44_PHASE6_HANDOFF.md`

The duality-rich (decodable) target for bare-metal Exp 44 Phase 6: **6.2** the cyclic period oracle (abelian shelf, D=1.000) and **6.4** the prime grating → Riemann zeros (Brick 1 recovered the first 10 in software). Predicted resonant peaks supplied as the silicon acceptance test; the non-abelian wall (D_8) is flagged as the boundary silicon should not be expected to cross. Tape SHA-256 lifecycle contract included. Explicitly a **Level-5 prediction handed to silicon for Level-6 isomorphism testing** — Exp 50 does not pre-claim the silicon isomorphism.

## Conclusion

The decoder's extractive boundary is located and characterized, and converted into two precisely-scoped questions: one conceptual (Mythos — barrier or frontier?), one physical (Exp 44 — does silicon host the decodable side?). Both are questions, not claims, kept at level 5.
