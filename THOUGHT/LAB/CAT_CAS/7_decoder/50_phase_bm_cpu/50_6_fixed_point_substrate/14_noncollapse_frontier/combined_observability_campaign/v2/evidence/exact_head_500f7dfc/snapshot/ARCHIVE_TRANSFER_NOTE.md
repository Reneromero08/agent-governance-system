# Archive Transfer Note

`phase6_v2_500f7dfc.tar` was produced from Git commit `500f7dfcd198e6e70dc3f999248aa61224d530cd` with `git archive` and transferred separately to the Phenom target at `/root/catcas_phase6_v2_500f7dfc/sealed/`.

The tar payload is intentionally not committed in this evidence directory because it is a 717,588,480-byte transport artifact. The committed evidence preserves its SHA-256 sidecar, recursive exported-file manifest, identity JSON, target archive verification log, and pre/post target manifest verification logs.
