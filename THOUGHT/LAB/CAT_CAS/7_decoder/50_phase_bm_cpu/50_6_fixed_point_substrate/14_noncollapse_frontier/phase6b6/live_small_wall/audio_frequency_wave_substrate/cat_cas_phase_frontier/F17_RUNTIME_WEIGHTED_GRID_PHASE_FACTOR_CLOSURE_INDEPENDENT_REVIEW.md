# M119 independent review

Classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Restoration class: `EXACT_ALGEBRAIC_RESTORATION`

Scientific source parent: `7bdbac7a8eb047b82bcd9171402188ae665a2cef`

## Independently reconstructed scope

The review reconstructed two deterministic public runtime-weight families on
binary square grids for `n=2,3,4`. The independent oracle imports neither the
M119 production module nor its M116/M118 phase backend. It agrees with the
production boundaries through three separate exact routes:

- direct full-assignment character enumeration;
- Gray-code enumeration with one changed-bit energy delta per assignment;
- exact row-character transfer.

The accepted production path stores public unary and pair factor cells rather
than a dense `2^(n^2)` global amplitude tensor. It evaluates `2^n` row states
and `(n-1)4^n` source-target transitions. The actual row-transfer interface
has exact ranks `4,8,16`. Independent determinant evaluation under the
order-seventeen image `zeta_17 -> 72` in `F103` is nonzero for every tested
case. These are ranks over `Q(zeta_17)`, not over rational coordinates or
internal split lanes.

The production transaction projects one final cyclotomic scalar. Reverse
phase actions and inverse preparation shears recover the factor seed exactly;
seed unload returns the original backing to zero. A held-out runtime family
then reuses the same backing and agrees with a fresh carrier. The independent
oracle separately reexecutes factor-cell preparation, root actions, reverse
actions, seed recovery, zero unload, and backing identity.

## Controls and repaired review findings

The final source rejects or detects premature projection, missing and wrong
inverse operations, noncommuting inverse reordering, resident mutation, a null
carrier, a mismatched topology fingerprint, and mismatched projection family
or fingerprint. It also verifies a separable rank-one program, a boundary
change after removal of a row-interface edge, rejection of a false rank cap,
and absence of snapshot reload.

Before sealing, review found nine scope or accounting defects. The package was
repaired to:

- use a Gray-delta 17-bin baseline while retaining direct enumeration only as
  an independent reference;
- disclose the full assignment-tree build and lack of order optimality in the
  three-order MTBDD sweep;
- scope transfer liveness as a conservative selected-value sum, including
  frontier, accumulator, term, factor, and product values, with possible alias
  double-counting and Python object internals excluded;
- count stored phase, family, and fingerprint strings as UTF-8 payload;
- report public topology-compilation and runtime-binding logical counts;
- enforce family and topology ownership at final projection;
- disclose `2^n` row-state and `4^n` per-interface transition enumeration;
- certify the actual row-transfer interface; and
- remove Pareto-optimality and ambiguous exponent wording.

Fresh production, oracle, and observational benchmark executions pass after
those repairs. No remaining claim-breaking defect was found.

## Classical comparison and ceiling

The evaluated compact classical set contains the identical exact row transfer,
the Gray-delta 17-bin histogram, and a three-order observed reduced MTBDD
sweep. This set is not claimed exhaustive or Pareto-optimal. The direct
zero-field planar-Ising Pfaffian condition is inapplicable because the tested
programs have nonzero field residues; broader matchgate or holographic
reductions are not ruled out.

The strict result is limited to two public runtime families, grid sizes
`n=2,3,4`, Linux direct-process software, exact public phase-factor mutation,
one final scalar boundary, exact reverse/unload, and same-backing held-out
reuse. It does not establish compact closure across growing treewidth, a
distinct phase resource, computational advantage, Small Wall crossing, CATVM
custody, catalytic inference, physical waveform or silicon execution,
replacement of physical bits with pi, or unbounded computation.
