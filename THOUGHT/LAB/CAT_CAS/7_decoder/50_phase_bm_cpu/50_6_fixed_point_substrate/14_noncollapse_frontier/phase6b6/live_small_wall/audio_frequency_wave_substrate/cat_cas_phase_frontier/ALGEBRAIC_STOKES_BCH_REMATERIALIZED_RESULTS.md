# Rematerialized Stokes BCH Phase Results

Evidence:

```text
/tmp/stokes-bch-rematerialized-final.3Xj1ap/evidence
```

Accepted bounded claim:

```text
BOUNDED_TOPOLOGY_REMATERIALIZED_NONCOMMUTING_STOKES_BCH_CHARACTER_PHASE_CLOSURE_WITH_RESTORATION_AND_REUSE
```

The phase backend executes 72 nonzero public BCH tensor words through word
grade six. It keeps 116 final coefficient cells and reuses 116 scratch
coefficient cells, for 8,352 unit phases and 133,632 logical packed bytes.
The maximum live scratch depth is six; retained Lie-word history is zero.
A same-output dual-prime grade-six signature needs 232 logical residue
bytes; actual classical allocation is unmeasured. The public word letters
and rational coefficients are accounted separately.

The non-emitting verifier compares all 464 primary/reuse dual-prime cells.
Maximum final root error is `2.949e-12`. Correct restoration is
`4.554e-13`, and an unrelated reuse program restores the same carrier within
`6.054e-13`. Missing and wrong inverses each leave residual `1.993`.
Swapped module order changes the coefficient boundary.
The executed snapshot sham transfers 133,632 logical bytes when creating and
again when reloading its baseline, restores no catalytic generation, and can
then run the reuse program. The null-carrier path fails closed.

The independent exact rational diagnostic continues through word grade ten.
The unique harmonic representatives have middle catalecticant ranks:

```text
3,3,5,5,7,7,9,9,11,11
```

Thus topology-derived reversible rematerialization removes retained
Lie-word history, but it does not remove growth in the declared final
signature. A same-output dual-prime coefficient signature through grade ten
requires 720 logical residue bytes. The 64-byte direct point evolution has
different output semantics.

This is not fixed-rank closure, an arbitrary compact-representation lower
bound, computational advantage, Small Wall crossing, or physical execution.
The next phase-owned obstruction is the final signature itself: find a
relation-preserving nonseparable quotient, or a phase coupling law whose
useful boundary is not equivalent to compact classical point evolution.
