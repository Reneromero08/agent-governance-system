# F103 C17 Two-Fiber Paley Weyl-Chirp Fusion-Escape Independent Review

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Restoration class: `EXACT_ALGEBRAIC_RESTORATION` for the resident two-port
carrier. Public operands, transient deltas, dense oracle matrices, and Fourier
support scans carry `NO_RESTORATION_CLAIM`.

## Reconstructed mechanism

The production carrier stores adaptive coefficients indexed by source fiber,
target fiber, cyclic displacement, and C17 Fourier mode. Public quadratic
chirps act by the exact Weyl update
`(d,m) -> (d,m-2kd)` with multiplier `omega^(-k d^2)`. Native relation
composition is a twisted convolution with multiplier `omega^(m2 d1)`, while
intersection is Fourier-mode convolution at fixed displacement. Reversible
triangular shears couple two unresolved relation ports and expose only one
declared final boundary scalar before exact reverse execution.

The independent oracle imports no production module. It reconstructs the same
public descriptors directly as exact 34-by-34 vertex matrices over F103,
executes ordinary matrix multiplication and Hadamard product, and recovers
Weyl support through a separately written inverse C17 Fourier transform.

## Executed evidence

- Primary and alternate public program families at depths 1, 2, 4, 8, and 16.
- All ten boundaries, complete-state SHA-256 commitments, checkpoint support
  histories, final supports, exact reversals, backing identities, and
  restoration generations match the independent dense oracle.
- Twisted composition and phase-mode intersection match dense matrix semantics;
  the chosen F103 root has exact order 17 and chirp inversion is exact.
- Missing, wrong, and reordered inverses fail; module reordering changes the
  boundary; premature projection and a null family are rejected.
- Generation-two cross-family reuse on the same backing matches a fresh
  carrier without snapshot reload.

## Resource law, negative result, and ceiling

The predecessor uses twelve fused coefficients per port. Its expanded Weyl
seed has 68 active cells per port, or 136 across two ports. After the first
chirp-coupled update, every observed state has 2,278 to 2,308 active cells out
of the 2,312-cell two-port Weyl capacity. The independent oracle confirms that
a chirped public relation is x-dependent within a fixed displacement, so it
cannot remain in the fixed-12 Paley fusion chart.

This is a bounded fusion-escape and near-density no-go, not exact full-support
saturation. The strongest compact classical method is the identical adaptive
Weyl recurrence and therefore has the same state and operation law. Dense
matrices are the independent semantic oracle, not a weak comparison baseline.
The evidence is limited to F103, C17, two fibers, two fixed public descriptor
families, depths through 16, and direct-process Linux software. It establishes
neither growing-order closure nor a compact algebra below full matrix
dimension, CATVM custody, a distinct phase resource, computational advantage,
a Small Wall crossing, physical waveform execution, physical-bit replacement,
or unbounded catalytic computation.
