# Phase-QEMU V3 Ising-holonomy findings

## Result

```text
IDEAL_EXACT_ISING_MTC_TRIANGULAR_ADJACENT_BRAID_PREPARATION_CONTROLLED_TRANSPORTED_MAJORANA_PAIR_HOLONOMY_FINAL_ONLY_BOUNDARY_FUNCTIONAL_RESTORATION_AND_REUSE_AT_N4_N8_N12_N16_WITH_CONTIGUOUS_CUT_RANKS2_4_8_AND_COMPACT_SIGNED_PAIRING_RESOURCE_KILL
```

Verification classification:

```text
INDEPENDENTLY_VERIFIED_STRICT_SCOPE
```

Verification level:

```text
SEPARATE_REFERENCE_PARITY
```

Restoration classification:

```text
EXACT_ALGEBRAIC_RESTORATION
```

Restoration scope:

```text
FUNCTIONAL_EXACT_VALUE_RESTORATION_AND_REUSE_WITHOUT_SAME_BACKING
```

Claim ceiling:

```text
IDEAL_DETERMINISTIC_EXACT_SOFTWARE_ISING_MTC_CONTROLLED_HOLONOMY_DIAGNOSTIC_AT_N4_N8_N12_N16_ONLY
```

This is an exact deterministic software backend diagnostic. It is not QEMU
device execution, CATVM custody, a physical anyon experiment, or an escape
from M257.

The `SEPARATE_REFERENCE_PARITY` level applies to the exact operator algebra,
prepared states, ranks, holonomy boundaries, mixed-loop factorization law, and
public-adjoint equality. The independent reference derives the coherent-copy
law from exact branch vectors but does not execute a second transaction state
machine. Production response ordering is therefore
`PACKAGE_SELF_REVIEW_SOURCE_AUDITED`. The dephasing sham is an analytic exact
control; it does not materialize or propagate a density matrix.

## Exact family

For `n=4r` sigma anyons, the package represents `2r` pair-charge modes in the
fixed-even-parity sector and executes only adjacent Majorana exchanges:

```text
C_j = (I - gamma_j gamma_(j+1)) / sqrt(2).
```

The public triangular word is generated by concatenating
`[2t+2,2t+1,...,t+3]` for `t=1...2r-3`. The exact words are:

```text
n=8:  [4]
n=12: [4,6,5,8,7,6]
n=16: [4,6,5,8,7,6,10,9,8,7,12,11,10,9,8].
```

Both independent implementations obtain:

| sigma anyons | fusion-sector dimension | full allocated cells | support | contiguous-center rank | prep braids |
|---:|---:|---:|---:|---:|---:|
| 8 | 8 | 16 | 2 | 2 | 1 |
| 12 | 32 | 64 | 4 | 4 | 6 |
| 16 | 128 | 256 | 8 | 8 | 15 |

The cut is the ordinary contiguous cut after pair-charge mode `r`. The ranks
are measured from actual exact amplitude flattenings, not inferred from the
descriptor. A separate `n=4` smoke verifies the orientation convention:
vacuum has local pair phase `+1`, `C_2 C_2` prepares `|11>` up to an exact
Gaussian unit and changes the phase to `-1`, and the public adjoint restores
vacuum exactly.

## Controlled transported holonomy

The accepted observables are not bare, framing-free subset loops. They are:

```text
BRAID_TRANSPORTED_MAJORANA_PAIR_PARITY_HOLONOMY
```

with explicit transport inherited from the public braid:

```text
L0 = -i gamma_3 gamma_(2r+1) =  W S_2 W^dagger,  eigenvalue +1
L1 = -i gamma_4 gamma_(2r+2) = -W S_3 W^dagger,  eigenvalue -1.
```

Generation one returns bit `0` from `L0`; generation two consumes the exact
restored value without a second preparation and returns bit `1` from distinct
`L1`. Production explicitly evolves the coherent probe path, copies only a
deterministic response, unlatches the probe, applies the public-derived
inverse, verifies exact restoration, and only then releases the bit. No stored
charge, expected phase, answer table, or saved baseline implements the result.

The implementation nevertheless proves only immutable-value restoration and
reuse. It does not preserve or identify one actual resident backing.

## Decisive controls

The mixed operator `M=-i gamma_3 gamma_4` has exact `+/-` weights `1/2,1/2`
for all three scaling fixtures. Retaining a coherent output copy leaves a
nonfactorized carrier/response: each fixed-bit restored form has fidelity
`1/4`, restored-carrier marginal weight is `1/2`, and the best factorized form
with an arbitrary response state has fidelity `1/2`. The response stays
locked. Without the retained copy, latch and unlatch restore exactly.

The same-sector scramble

```text
K   = -i gamma_2 gamma_(4r-1)
U_K = (I - gamma_2 gamma_(4r-1)) / sqrt(2)
```

changes the carrier ray with overlap squared `1/2` while commuting with both
accepted holonomies. Both outputs remain `+1,-1`; its adjacent-braid synthesis
matches the direct action; and the public adjoint restores exactly. This is
the decisive evidence that the boundary sees only compact pair-charge data,
not the growing internal amplitude geometry.

Missing and wrong-loop inverses lock the response. Nonzero framing is rejected
before mutation. The analytic probe-path dephasing law destroys the
deterministic boundary.
Reverse orientation agrees because the selected pair parities are Hermitian
involutions.

## Classical closure and route kill

The exact dense fusion vectors grow and the chosen contiguous-cut rank reaches
eight, but the entire public family remains a signed perfect-pairing Gaussian
state. The controlling comparator stores an `O(n)` signed Majorana frame and
pairing, updates an adjacent exchange in `O(1)`, answers an accepted pair
holonomy in `O(1)` after validation, and obtains the selected cut rank by an
`O(n)` crossing-edge count. A general Ising braid state has an `O(n^2)`
covariance or stabilizer representation with polynomial updates. The literal
public preparation has `O(n^2)` exchanges, while the final pairing can be
compiled directly in `O(n)`.

Therefore the accepted disposition is:

```text
TRIANGULAR_ISING_BRAIDS_AND_TRANSPORTED_PAIR_HOLONOMIES_REMAIN_COMPACTLY_TRACKABLE_BY_AN_O_N_SIZED_SIGNED_MAJORANA_PAIRING
```

The Ising route is promoted only as a bounded coherent-holonomy machine-law
calibration and killed immediately as a computational-resource candidate.
This is consistent with the efficient classical simulation of Ising/Majorana
braiding; it is not a no-go for general non-Abelian anyons or physical phase
machines.

Primary background:

- D. A. Ivanov, *Non-Abelian Statistics of Half-Quantum Vortices in
  p-Wave Superconductors*, <https://arxiv.org/abs/cond-mat/0005069>.
- P. Bonderson, K. Shtengel, and J. K. Slingerland, *Interferometry of
  non-Abelian Anyons*, <https://arxiv.org/abs/0707.4206>.
- A. Ahlbrecht, L. S. Georgiev, and R. F. Werner, *Implementation of
  Clifford gates in the Ising-anyon topological quantum computer*,
  <https://arxiv.org/abs/0812.2338>.
- B. M. Terhal and D. P. DiVincenzo, *Classical simulation of
  noninteracting-fermion quantum circuits*,
  <https://arxiv.org/abs/quant-ph/0108010>.

## Resource scope

Production allocates full occupation arrays of `4,16,64,256` cells for the
`n=4,8,12,16` cases. The explicit carrier+path+response transaction arrays are
`16,64,256,1024` exact coefficient cells. Dense rank verification allocates
`4,16,64,256` Gaussian cells. Public preparation braid counts are
`2,1,6,15`. Retained dynamic inverse history is zero.

This is component accounting, not a complete live-payload or process measure.
Immutable intermediate allocations, Python objects, allocator/RSS, hashing,
serialization, QEMU traffic, physical preparation, energy, gap, noise,
leakage, precision, bandwidth, latency, visibility, calibration, and shot cost
are incomplete or unmodeled. Resource verification remains
`PACKAGE_SELF_REVIEW`.

## Successor

The next factoring-eigenphase mechanism must make the deterministic phase
depend on a growing interacting relational invariant that is not a finite
superselection label and is not compactly tracked by a Gaussian pairing,
stabilizer state, or equivalent polynomial recurrence.

Before constructing a larger anyon backend, the next result should test the
broader finite-superselection obstruction: a closed deterministic probe loop
in a fixed modular tensor category reads a simple-object charge label even
when the enclosed fusion-multiplicity space grows. If that obstruction closes,
the architecture must move to genuinely interacting, non-Gaussian scattering
or Floquet eigenphase dynamics rather than another topological-charge loop.

## Durable evidence

- `evidence/PHASE_QEMU_V3_ISING_MONODROMY_DIAGNOSTIC.json`
- `evidence/PHASE_QEMU_V3_ISING_MONODROMY_SEPARATE_REFERENCE.json`
- `tests/qualify_ising_anyon_controlled_monodromy.py`
