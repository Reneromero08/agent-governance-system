# CATVM Bosonic Givens Results

## Result

The topology-compiled bosonic Givens phase closure now runs behind an enforced
separate-process CATVM boundary. The accepted bounded claim is:

```text
BOUNDED_CATVM_ENFORCED_TOPOLOGY_COMPILED_BOSONIC_GIVENS_HIDDEN_OCCUPATION_COMPOSITION_WITH_ACTUAL_INVERSE_RESTORATION_AND_REUSE
```

The tested ceiling is:

```text
LINUX_X86_64_LITTLE_ENDIAN_SAME_UID_AF_UNIX_SEQPACKET_GRID17_FOUR_ROTOR_DEPTH8_TESTED_NONZERO_CHIRP_COMPLEX128_SOFTWARE_ONLY
```

Evidence was qualified at:

```text
/tmp/catvm-bosonic-givens-accepted.j4xxOA
```

The evidence manifest binds the base phase source, CATVM service tail,
protocol-only controller, qualifier, generated service source, exact executed
binary, and result. All manifest entries verify.

## Enforced transaction

The backend is non-dumpable and accepts one same-UID peer through a mode-0600
Unix `SOCK_SEQPACKET` socket. The controller imports only the fixed protocol.
It cannot import or call the phase backend.

The transaction:

1. creates the 285-cell necklace carrier;
2. expands the free closure to 4,845 permutation-symmetric occupation
   amplitudes and applies the first 68 of 136 topology-compiled Givens
   rotations;
3. retains that actual occupation vector across a command boundary;
4. denies attempted projection while the vector is resident;
5. continues the remaining 68 rotations on the same resident vector;
6. closes back to the 285-cell necklace carrier and releases the hidden stage;
7. projects only the seven-value final invariant boundary;
8. applies the actual inverse depth-seven through depth-zero on the same
   carrier allocation;
9. verifies restoration, then runs an unrelated depth-two tag-three program on
   the actual restored carrier.

No occupation amplitude is decoded or serialized. Backend stdout and stderr
are empty. The protocol contains zero intermediate complex values and zero
intermediate bytes. The result boundary is retained by the controller outside
inverse history.

## Restoration, reuse, and controls

- primary restoration error: `8.446068614196007e-15`
- primary restoration generation: `1`
- carrier backing allocation preserved: `true`
- unrelated reuse restoration error: `9.27279516256988e-15`
- reuse restoration generation: `2`
- missing inverse error: `1.4040897828772652`
- wrong inverse error: `1.3464039441456188`
- reordered inverse error: `1.0750797873027769`
- intermediate projection: `DENIED`
- null carrier: `DENIED`

The reordered control is applicable because the tested forward schedule is
noncommuting.

## Resource account

The accepted in-place path performs `15,917,440` native polynomial block
operations. The service-local forward-only baseline and snapshot sham each
perform `7,958,720`; the difference is the accepted path's actual inverse.
Each matched arm uses two 16-byte requests and two 124-byte responses, or 280
logical protocol bytes. The full control run uses 1,960 logical protocol
bytes.

Explicit payloads are:

- carrier creation: 4,560 bytes
- resident carrier: 4,560 bytes
- non-restorative verification baseline: 4,560 bytes
- hidden occupation state: 77,520 bytes
- public topology: 10,532 bytes
- Givens plan: 4,624 bytes
- polynomial block scratch: 211 bytes
- conservative compiled plan: 19,867 bytes
- maximum explicit service payload: 102,007 bytes
- maximum service plus one packet: 102,147 bytes
- final projection: 56 bytes
- retained inverse history: 0 bytes

Kernel socket-buffer payload and host allocator metadata are not bounded by
this experiment. The snapshot sham creates and reloads a 4,560-byte carrier
copy. The accepted restoration reloads zero snapshot bytes.

The direct comparison is a service-local forward-only matched phase baseline,
not a warm direct-process benchmark. No cold-start comparison is used to
claim leverage.

## Scientific boundary

This establishes enforced custody for the automatically factorized hidden
occupation intermediate, actual inverse restoration, and actual restored
carrier reuse.

It does not establish cross-UID secrecy, microarchitectural secrecy, a
distinct phase resource, computational advantage, a Small Wall crossing,
physical waveform or silicon computation, or unbounded catalytic computation.
The matched classical bosonic Givens method is identical.

The surviving obstruction is:

```text
MATCHED_CLASSICAL_BOSONIC_GIVENS_IDENTITY_AND_POLYNOMIAL_OCCUPATION_SCRATCH
```

The selected phase-owned successor is:

```text
SYMMETRY_PRESERVING_NECKLACE_GENERATOR_CLOSURE_WITHOUT_OCCUPATION_EXPANSION
```

It must act directly on the 285-cell unresolved necklace carrier, preserve
exact global-rotation closure, restoration, and reuse, and account for any
Krylov, polynomial, or generator workspace rather than moving the 4,845-cell
occupation expansion elsewhere.
