# M252 focused independent review

Decision: `PASS_STRICT_SCOPE`

Classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `SEPARATE_REFERENCE_PARITY`

Restoration class: `EXACT_ALGEBRAIC_RESTORATION`

Scientific source head: `27ba6c975e1451f66ef9c202a77fb4bb88c0bb05`

The focused read-only review independently reconstructed exact arithmetic in
`Q[zeta_8]/(zeta_8^4+1)`, the two coherent order branches, and the boundary

```text
2 <VU psi / sqrt(2) | UV psi / sqrt(2)>
  = <psi|U^dagger V^dagger U V|psi>.
```

The accepted suite fixes `psi=|0>`. The `X,Z` pair returns the central phase
`-1`, the commuting `T,Z` pair returns `+1`, and the `H,T` pair returns the
noncentral exact `Q(zeta8)` value `(1+conjugate(zeta8))/2`. The standalone
polynomial-quotient oracle agrees with both the dense two-by-two commutator and
the explicit two-branch overlap for all 16 declared public gate pairs.

The abstract Unix-socket CATVM retains one actual four-cell branch backing, a
two-cell scratch backing, and two typed consumer receipts. Only the final
commutator scalar is returned. Both branch actions are inverted in reverse
dependency order on the same backing, canonical state is checked exactly, and
descriptor-distinct generation-two reuse agrees with fresh execution. Partial,
post-projection, and disconnect paths restore before rejection or response
loss; no snapshot or baseline reload occurs.

Exact order dephasing removes the off-diagonal boundary, equal branch orders
give one, omitting one branch action changes the boundary, and a common global
phase cancels only at the final overlap. Missing, completed wrong, and
applicably reordered inverses fail restoration. Custody, descriptor mutation,
malformed identifier, premature projection, dirty scratch, null, snapshot,
debug, and no-smuggle controls pass.

The initial package used a nonminimal two-vector comparator. The repaired
strongest transferable descriptor-level baseline streams one two-component
target through `V,U,V^dagger,U^dagger`, using two resident field cells plus two
reusable scratch cells, four gate actions, and 16 multiply terms, with no CATVM
restoration. It agrees with both independent oracles for every declared pair.
The fixed evidence fixtures also admit an `O(1)` validated invariant. These
comparators are smaller than the full CATVM lifecycle.

Field-cell, operation, receipt, protocol, restoration, and reuse counts are
reported at `PACKAGE_SELF_REVIEW`. Exact coordinate-payload and whole-process
live peaks remain explicitly uninstrumented, and Python object, allocator,
socket, hash, serialization, and RSS costs are excluded as nonzero.

The result is limited to one hidden two-branch order port, one fixed-zero target
qubit, and pairs from `X,Z,H,T` in exact software. It does not establish a
physical quantum switch or indefinite causal order, oracle/query separation,
general process-matrix execution, a resource unavailable to compact classical
software, computational advantage, Small Wall crossing, inference, physical
waveform execution, physical-bit replacement, or unbounded catalytic
computation. The route is retired after this bounded calibration rather than
extended with more order fixtures or target width.
