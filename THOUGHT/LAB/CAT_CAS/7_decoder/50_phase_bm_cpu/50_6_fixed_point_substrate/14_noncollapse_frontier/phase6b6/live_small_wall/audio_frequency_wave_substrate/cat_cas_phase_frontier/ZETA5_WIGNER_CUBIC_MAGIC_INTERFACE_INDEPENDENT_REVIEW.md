# M238 Focused Independent Review

Disposition: `PASS_STRICT_SCOPE`

The review inspected the production Wigner carrier, the standalone exact-
amplitude reference, the qualifier, and regenerated sealed outputs. It did not
treat package prose or the sealed classification as proof.

The accepted mechanism is bounded exact software over `Q(zeta_5)` using the
Gross odd-prime discrete Wigner convention at `p=5`. Clifford modules act as
phase-space permutations. Cubic diagonal phases use a formula-generated local
kernel rather than a retained answer table. Every declared cubic circuit at
interfaces `n=1,2,3` has positive exact Wigner negative mass, while the matched
quadratic/Clifford sham circuits remain exactly nonnegative.

The standalone reference imports neither M238 nor M237 production. It evolves
the `5^n` exact amplitudes and reconstructs the final Wigner function in stream
order. It agrees with the resident Wigner carrier on a one-way commitment to
every final Wigner cell, the selected boundary, exact negative mass,
normalization, purity, controls, restoration, and reuse. The reference retains
no `25^n` Wigner grid.

The transaction preserves the actual resident and scratch backings, retains
only the permitted final aggregate through inverse execution, restores the
canonical state exactly, advances generation one to generation two, and reuses
the same restored carrier for a descriptor-distinct circuit. Missing,
reordered, and wrong inverses fail; the wrong-inverse attack completes the
remaining inverse schedule before testing release. Full-vector and premature
projection, stale generation, descriptor forgery, wrong custody, null carrier,
and dirty scratch are rejected.

The decisive resource result is negative for this representation. The Wigner
carrier grows from `25` to `625` to `15,625` resident field cells, with an
equal-sized scratch. The strongest implemented cubic comparator retains only
`5`, `25`, and `125` exact amplitudes and streams the requested Wigner
aggregate. At `n=3`, the Wigner carrier records `66,586` exact payload bits,
while the two amplitude families record `1,000` and `980` bits. Resource
figures remain `PACKAGE_SELF_REVIEW`; Python/container/allocator/hash/RSS costs
and complete whole-transaction liveness are explicitly excluded, not zero.

Strict ceiling: two public circuit families at `p=5`, interfaces `n<=3`, and
direct-process logical custody. Exact Wigner negativity establishes a
stabilizer-relative magic witness, not a resource unavailable to compact
classical software. The package does not establish interface synergy,
tensor-product nonfactorization, general scaling, fixed-width compactness,
computational advantage, CATVM custody, Small Wall crossing, catalytic
inference, physical waveform execution, physical-bit replacement, or unbounded
computation.

Reviewed source hashes:

```text
production           2f5df438ff01276792cfb6f18eeb3d1c3a2bd9d101991ab17632aa82ce49419c
standalone reference dcfc59e2e874ab68838707bbb2900affeb19e06c97007382fe33e8ebf339f350
qualifier            a50612d19080a87863a8c59a1f9f52a1b289caa4cf6b60fdf0f0d36c593cf6a2
sealed result        a686b410aac1c09b7f2c19ecbe2bca2d4ee36727916adbe5a702be90f8b6198f
sealed reference     855089b120c06eddc9a2e5b4945fae343b55c1cc01c5535b5e870556888f6ebc
```
