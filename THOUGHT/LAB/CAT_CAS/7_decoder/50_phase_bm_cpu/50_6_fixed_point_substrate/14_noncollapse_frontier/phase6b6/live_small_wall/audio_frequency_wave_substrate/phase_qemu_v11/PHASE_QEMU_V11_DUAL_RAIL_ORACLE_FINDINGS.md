# Phase-QEMU V11 dual-rail oracle findings

## Verdict and authority

The compiled Phase-QEMU V11 reintegration checkpoint passes its recorded QEMU
10.2.4 qtest/QMP qualification.  This is a compiled formal-machine result.  It
is not a physical observation, a physical coherent-oracle implementation, a
carrier-custody observation, query separation, a total-resource advantage, an
M257 escape, or replacement of a bit with physical phase.

The canonical bounded claim is:

```text
COMMON_PHASE_QEMU_V11_SWAPPABLE_BACKEND_DEVICE_EXECUTES_TWO_LATE_BOUND_DUAL_RAIL_NUMBER_EIGENSPACE_DISPERSIVE_KICKBACK_QUERIES_ON_ONE_HIDDEN_CARRIER_REFERENCE_STATE_AND_RELEASES_ATOMIC_RECEIPTS_ONLY_AFTER_EXACT_COMPLETE_RETURN_WHILE_OPEN_AND_EXTERNAL_BACKENDS_FAIL_CLOSED
```

Claim ceiling:

```text
COMPILED_QEMU_10_2_4_PCI_DEVICE_WITH_EXACT_QOMEGA_IDEAL_BACKEND_TEST_ONLY_PRIVATE_PROVIDER_AND_ANALYTIC_OPEN_EXTERNAL_STUBS_NO_PHYSICAL_ORACLE_COHERENT_PORT_CUSTODY_QUERY_SEPARATION_OR_ADVANTAGE
```

Restoration classification:

```text
EXACT_ALGEBRAIC_RESTORATION
```

Restoration scope:

```text
EXACT_HIDDEN_96_DIMENSION_QOMEGA_DUAL_RAIL_CARRIER_REFERENCE_AND_TWO_CLIENT_REFERENCE_RETURN_ON_ONE_RESIDENT_QEMU_ALLOCATION_FOR_IDEAL_BACKEND_ONLY_WITH_NO_PHYSICAL_SAME_MODE_CUSTODY
```

Disposition:

```text
COMMON_DEVICE_REINTEGRATION_ESTABLISHES_BACKEND_NEUTRAL_LIFECYCLE_AND_EXACT_IDEAL_MACHINE_LAW_BUT_TEST_ONLY_PRIVATE_BINDING_AND_EQUAL_ACCESS_DIRECT_PHASE_SHADOW_PRECLUDE_PHYSICAL_OR_RESOURCE_PROMOTION
```

The next physical-integration target remains:

```text
AUTHENTICATED_EXTERNAL_DUAL_RAIL_DISPERSIVE_ORACLE_ADAPTER_WITH_COHERENT_CLIENT_PORT_REFERENCE_PRESERVATION_LATE_BOUND_PRIVATE_CONTROL_AND_TOTAL_RESOURCE_CERTIFICATION
```

The controlling M257 guardrail remains:

```text
EQUAL_ACCESS_EXACT_DETERMINISTIC_SOFTWARE_FORWARD_SHADOW_MUST_NOT_BE_COUNTED_AS_A_PHASE_RESOURCE
```

## Frozen evidence identity

The repaired source, compiled binary, runner, reference, and receipt are pinned
as follows:

| Item | Result or SHA-256 |
| --- | --- |
| Compiled build and qtest status | `PASS_COMPILED_COMMON_PHASE_QEMU_V11_REINTEGRATION_BUILD_AND_QTEST` |
| qtest/QMP status | `PASS_PHASE_QEMU_V11_QTEST_QMP_EVIDENCE` |
| QEMU version | `10.2.4` |
| QEMU binary | `02543d7c7df868c19c4fcc040c9f0717d99b4f9f2786c677f4621debe9e7ea40` |
| V11 C source, package and installed | `84c2ec576ae54b298046fadcda719dcb4c2e97bbe31aa0ac3c77ae5455027771` |
| Installer | `0d7aadbf5088cf124544a53c16ae895a3b759255cf01510e9983b7cf85dfe92e` |
| Device contract | `2f1eb232a09435176a3cc57ab3d1182226227c700323a777593f24e7a78ee6a3` |
| Build receipt | `7e862a5f4dacbd69573ce3361b27035542b14661b7f45fcd3255cff21b74cab4` |
| qtest runner | `fa70a83c24cb9bd9ffc78e6cf07ed3c21dc99d342c6302ac1482dd5df48b47dd` |
| qtest deterministic payload | `2286987541ebf283c1ed6733b750430fd09f8c978d32dbd9e75a6caf4f9ae49e` on two complete runs |
| Independent-reference source | `b9791716c6d973c0e119e3666e960efed0f84e16676c74b923e198b4930c1d3a` |
| Independent-reference stdout | `020dc7235bb43d785553f587be0b43453fb310dc252f79cb17901c4765219b11` |
| Independent-reference claim payload | `263b791d7898195f94aad9e6f5c7033c94b6a0fc249a2172705333a5502a8d16` |
| Independent-reference checks | `27/27`, `PASS_INDEPENDENT_V11_DUAL_RAIL_EXACT_NOISY_REFERENCE` |

The integrated V11 source hash equals the package source hash.  The QEMU build
contains exactly one V11 Kconfig entry and one V11 Meson entry while retaining
the independently buildable V0 and V1 devices.  The repaired incremental
compile and link passed.  Two complete qtest/QMP runs produced the same
deterministic payload hash, and QEMU stdout and stderr were empty.

## Architecture finding

The architectural distinction is now evidence-backed and must remain sharp:

- V0 and V1 are actual compiled Phase-QEMU PCI devices.
- V2 through V10 are standalone mechanism-search or digital-twin packages.
  Their names and algebra do not constitute common-device integration.
- V11 is the compiled reintegration checkpoint: QOM type
  `phase-qemu-v11`, PCI identity `1234:11fb`, revision one, a 4096-byte BAR0,
  magic `PH11` (`0x50483131`), and ABI `0x00010000`.

The V11 core owns lifecycle transitions, descriptor sealing, atomic boundary
release, allocation/custody receipts, resource sealing, reuse policy, and
migration lineage.  Backends supply `lease_preflight`, prepare, client-supply,
execute, `verify_return`, and sanitize behavior behind one guest-visible ABI
and an immutable backend ID; they cannot set lifecycle or release a response.
Therefore a mechanism qualifies for this architecture checkpoint only when it
is compiled and exercised through this common device contract; a standalone
twin cannot qualify.

The recorded architecture gates are
`qemu_device_source_integrated=true`, `qemu_device_implemented=true`,
`common_guest_visible_contract_source_integrated=true`,
`common_guest_visible_contract_compiled=true`,
`common_guest_visible_contract_exercised=true`, and
`reintegration_gate_passed=true`.  Conversely,
`standalone_twin_qualifies=false` and
`physical_oracle_adapter_implemented=false`.

The installer is a source-integration gate, not compilation evidence.  It
accepts only an explicitly named QEMU 10.2.4 tree, validates its own AST and
the device C structure, rejects PCI/QOM collisions, and preserves V0/V1 source
hashes and exact integration counts.  For Kconfig it cross-checks the complete
canonical block, `config PHASE_QEMU_V11` token, and every
`PHASE_QEMU_V11` symbol occurrence; for Meson it cross-checks the canonical
line, `CONFIG_PHASE_QEMU_V11` symbol, and `phase-qemu-v11.c` occurrence.  Any
partial or malformed entry, stray occurrence, or duplicate count fails closed.
`--check` also constructs and validates the prospective one-Kconfig/one-Meson
state entirely in memory and performs no writes.  Installer output therefore
leaves `qemu_device_implemented=0`,
`common_guest_visible_contract_compiled=0`, and `reintegration_gate_passed=0`
until the separate build and qtest receipt supplies those gates.

Every secret pair uses the same public descriptor:

```text
0x50313144  0x00010008  0x00000002  0x00020102
0x00020011  0x00030021  0x00000003  0x00010001
```

The two residues are bound after `ARM_PRIVATE` through write-once, host-only
qtest-provider QOM properties.  They are absent from BAR0, the descriptor,
VMState, receipts, and logs.  This proves the no-smuggle software boundary; it
does not provide an authenticated physical secret provider or an access
restriction outside M257.

## Exact ideal-machine finding

The ideal backend materializes a hidden full `96 x 96` density matrix over
`Q(omega)`, with `omega^2 + omega + 1 = 0`, plus an independent full scratch
matrix.  The tensor dimensions are

```text
C_A, R_A, C_B, R_B, K, R_K = 2, 2, 2, 2, 3, 2.
```

`K` has `vac`, rail `a`, and rail `b`; the formal carrier/reference state is
`(|a,0> + |b,1>)/sqrt(2)`.  Together with two fresh Bell-Choi client/reference
pairs, preparation has eight equal pure components and density denominator
eight.  It has one-photon formal support, not a measured energy or physical
Fock-state preparation.

Each late-bound residue `s` acts on every density cell through the declared
controlled-number law

```text
omega ** (s * (c_ket*N_ket - c_bra*N_bra)).
```

The implementation does not write a precomputed client answer.  Before exact
response commit it verifies the complete joint factorization, both client and
reference Choi channels, exact `K,R_K` return, clear ports, factored
environment, sealed resources, stable backing, and unchanged resident
allocation.

`CMD_EXECUTE_ATOMIC` now exposes an internal lifecycle split without adding a
guest command.  The core sets `LIFE_EXECUTING` and keeps it set throughout the
backend `execute` callback; the ideal/open execution path evolves the hidden
density and returns `RETURN_NONE`, not an exact-restoration assertion.  Only
after successful evolution does the core enter `LIFE_VERIFYING_RETURN` and
invoke the distinct backend `verify_return` callback.  The core then owns the
returned classification, restoration/reuse flags, resource seal, and atomic
response commit.  Failure in either callback spends the generation and leaves
the boundary locked, so evolution and return certification are not collapsed
into one backend assertion.

## Lifecycle, custody, and reuse findings

All nine ordered pairs in `Z3 x Z3` passed on one QEMU instance and one
resident allocation.  The cumulative result is:

| Quantity | Recorded value |
| --- | ---: |
| Generations | 9 |
| Carrier/reference preparations | 1 |
| Fresh client supplies | 18 |
| Coherent query applications | 18 |
| Successful `BEGIN_REUSE` operations | 8 |

Generation one follows `LEASE -> PREPARE -> ISOLATE_SOURCE`.  Every reused
generation leases two fresh client ports on the same restored carrier, returns
to the prepared lifecycle, and executes `ISOLATE_SOURCE` again.  No reused
generation inherits source isolation from its predecessor, and there is no
second carrier preparation.

For every generation, the sixteen-word boundary remained all ones until both
outputs were held, exact return was certified, resources were sealed, and the
atomic response committed.  The status and boundary recorded `OUTPUTS_HELD`;
outputs remained held throughout response-ready and were released only by
`ACK_RESPONSE`.  Only then could reuse begin.  Preparation, return, output,
allocation, custody, and resource receipts are bound to the same resident
allocation and generation lineage.

At commit, the core snapshots the mutable cumulative control count; with the
other coordinates fixed, resource reads expose one response-local complete
sealed vector.  The digest covers every reported coordinate, including the
frozen controller count and explicit unknown sentinels.  The dedicated qtest
then issued a width-correct BAR write while the response was ready.  The access
was charged to the live control counter but rejected with `RESPONSE_LOCKED`;
it changed neither argument state, the sealed control count and resource
vector, the complete-vector digest, nor any boundary word.  A successful
`BEGIN_REUSE` is the only tested path that unseals the live vector for the next
generation.

## Snapshot, migration, and failure findings

The device-level snapshot command is rejected.  The separate migration case
used real QMP Unix live migration, reached `completed` on the original source,
and forced the first destination to `SHAM` with failed return, cleared
private-ready state, snapshot lineage, and a locked boundary.  It then migrated
that sanitized first-hop SHAM to a second destination through another QMP Unix
hop; the second source again reached `completed`, while the second destination
retained SHAM lineage and a locked boundary without reviving carrier state.
After QMP `system_reset` on the second destination, the test re-enumerated PCI
and remapped BAR0 before reading it.  The non-VMState migration latch preserved
`SHAM` across the second hop and reset; lease and reuse remained unavailable.
Thus neither migration chaining nor reset can sanitize migrated lineage into a
reusable carrier.  This is a software migration-sham control, not physical
continuity or custody.

Fault modes 1 through 7 all failed closed, spent the affected generation, set
return class `FAILED`, and left all boundary words locked:

| Fault | Injected condition | Required error |
| ---: | --- | --- |
| 1 | vacuum carrier | `CARRIER_REFERENCE_FAILED` |
| 2 | non-eigenstate carrier | `CARRIER_REFERENCE_FAILED` |
| 3 | differential rail coupling | `CARRIER_REFERENCE_FAILED` |
| 4 | port stuck | `PORT_NOT_CLEAR` |
| 5 | environment tag retained | `ENVIRONMENT_NOT_FACTORED` |
| 6 | state corruption | `INVARIANT` |
| 7 | resource unsealed | `RESOURCE_UNSEALED` |

Additional controls rejected wrong lifecycle order, wrong generation, missing
private binding, descriptor secret smuggling, and snapshot requests without
leaking a boundary.  A duplicate private QOM write was rejected while
preserving the original binding; the subsequently completed legitimate
transaction remained exact.  Invalid byte, word, and unaligned 64-bit MMIO
reads returned all ones and latched `BAD_ARGUMENT`; corresponding writes
latched `BAD_ARGUMENT`.  Lifecycle and generation did not change.

The final overflow repair reserves `UINT64_MAX` exclusively for the unknown
resource sentinel.  Width-correct BAR writes and accepted private QOM bindings
fail with `OVERFLOW` before their shared control counter can wrap or reach the
sentinel.  Cumulative Q40 action is preflighted for both atomic queries before
execution and cannot become the sentinel.  Allocation, arm-nonce, and response
transaction serial exhaustion is rejected before an identifier can wrap or be
reused; response-transaction exhaustion spends the generation without
releasing a boundary.  These checks preserve fail-closed receipt identity and
the distinction between a known counter and an unknown resource coordinate.

With the test provider disabled, service capabilities omit both the private
provider and test observer.  Private QOM writes and observer QOM reads return
`GenericError`; `ARM_PRIVATE` returns `BACKEND_UNAVAILABLE`, leaves the device
sealed, and releases no boundary.  Qualification-only observers therefore do
not become a service or guest interface.

## Backend classifications

| Backend | Qualified behavior | Ceiling |
| --- | --- | --- |
| `IDEAL_DUAL_RAIL` (`0x0b01`) | Exact `Q(omega)` density evolution, complete formal return, and exact reuse | Algebraic software model only |
| `OPEN_DUAL_RAIL` (`0x0b02`), zero noise | Forwards to ideal behavior and matches the same ABI | Parity path, not an independent physical open-system result |
| `OPEN_DUAL_RAIL` (`0x0b02`), nonzero model | Returns `APPROX_MODEL`, fails exact-reuse qualification, and does not assert complete environment return | Analytic classification stub; no Kraus, Lindblad, calibrated loss, or observed open dynamics |
| `EXTERNAL_ADAPTER` (`0x0b80`) | Its backend `lease_preflight` callback returns `BACKEND_UNAVAILABLE` before the core mutates lease or lifecycle state | No external adapter, coherent port, authenticated provider, or physical custody exists in V11 |

The core invokes the common `lease_preflight` operation and does not special
case the external backend ID.  The backend dispatch and fail-closed boundary
are therefore exercised through the swappable interface.  Only the ideal
formal backend establishes exact algebraic restoration.

## Equal-interface resource finding

The sealed resource vector reports a vector, not a favorable scalar score:

| Coordinate | Recorded ideal value or rule |
| --- | --- |
| Resident density | 9216 `Q(omega)` cells |
| Independent scratch | 9216 `Q(omega)` cells |
| Peak modeled working storage | 2,363,904 bits, including the 36-cell return-verification workspace |
| Integer coefficient precision | 64 bits |
| Logical secret-pair entropy | 4-bit lossless lower bound for nine pairs |
| Allocated private-provider state | 168 bits |
| Formal carrier photon number | 1 |
| Query applications | 2 per generation; 18 after nine generations |
| Control/controller words | 29 at generation one, then +30 per acknowledged reuse transaction |
| Cumulative control words | 29, 59, 89, 119, 149, 179, 209, 239, 269 |
| Compiler operations | `UINT64_MAX` = unknown |
| Construction operations | `UINT64_MAX` = unknown |

The unknown sentinel also applies to uninstrumented duration, port bandwidth,
output-hold time, physical mean energy, maintenance, allocation/build work,
authentication, and other whole-process physical costs.  It never means zero
or a measured maximum.  Ideal loss and dephasing parameters are zero only
inside the ideal model; they are not measurements of a device or environment.

The strongest equal coherent-access quantum comparator performs the identical
two late-bound queries.  A direct secret-controlled client-phase
implementation produces the same formal client action while omitting carrier
preparation and restoration.  An equal-interface coherent classical-wave
comparator must charge secret programming, reference generation, calibration,
phase/control precision, bandwidth, duration, losses, environment history,
custody, and reuse degradation under the same boundary rules.

For a generic `N`-entry order-`d` phase oracle, operational secret storage is
at least `(N-1) log2(d)` bits modulo global phase.  Exact deterministic fixed
processors require orthogonal programs of dimension at least `d^(N-1)`; two
independent programs pay the product dimension.  Public oracle tables retain
their construction, calibration, and amortization costs, including the table
cost in a Forrelation comparison.  M241/M242 public-law compilation and the
M257 exact deterministic equal-access shadow therefore remain controlling.
A restricted provider would change the premise, but the V11 qtest provider
does not establish such a physical restriction.

## Promotion decision

V11 promotes the dual-rail law only to a compiled common Phase-QEMU
device/backend machine law.  It closes the post-M268 reintegration defect and
preserves V0/V1 as real devices while preserving V2-V10 as standalone twins.
It does not promote the mechanism to a physical oracle or resource advantage.

Physical promotion requires the named external-adapter successor to prove an
authenticated late-bound private provider, a coherent client port, faithful
carrier/reference return, physical same-mode custody, calibrated finite energy
and gap, loss/dephasing and environment history, phase/control precision,
bandwidth, duration/action, reuse degradation, and equal-interface total
resources.  None of those observations is supplied by this compiled qtest
result.
