# M237 Focused Independent Review

Disposition: `PASS_STRICT_SCOPE`

The review inspected the production source, standalone exact-matrix reference,
qualifier, and regenerated sealed outputs. It did not treat the sealed result,
roadmap prose, or package classification as proof.

The reviewed mechanism is bounded exact software over `Q(zeta_5)`. A fixed
five-amplitude carrier undergoes normalized Fourier and cubic diagonal phase
operations with an exact common-denominator representation. The accepted
witness has exact destructive interference at the selected boundary while an
independently computed dephased control has probability `1/5`. No path or
assignment histories are enumerated.

The standalone implementation reconstructs the cyclotomic arithmetic,
formula-derived matrices, normalization, descriptor custody, inverse ordering,
and restored-carrier reuse without importing production. Across both declared
families and depths `2,4,8,16,32,64`, it agrees with production on final-vector,
amplitude, probability, restoration, and reuse commitments.

The first adversarial pass identified three narrow defects. The final reviewed
source repairs all three:

- the wrong-inverse attack substitutes the wrong last operation, completes all
  remaining declared inverses through index zero, and only then requires
  release failure;
- probability and its commitment are derived only after exact inverse and
  release, while the retained amplitude's four integer coordinates, one
  denominator exponent/material value, and retained one-way 32-byte vector
  commitment are reported and qualifier-checked;
- fixed bounded-width exact state is explicitly denied. The five logical cells
  conceal payload growth from `43` to `416` bits in family zero and from `45`
  to `418` bits in family one, with denominator exponent reaching nine.

The transaction restores the actual carrier and scratch backings exactly,
advances generation one to generation two for descriptor-distinct reuse, agrees
with a fresh carrier, and performs no baseline reload. Only commitments and
aggregate resource metrics leave the carrier; intermediate or full-vector
amplitudes are not serialized.

The strongest implemented comparator is the identical exact five-amplitude
recurrence, with the equivalent backward five-component row recurrence noted
for a single selected boundary. Resource figures remain
`PACKAGE_SELF_REVIEW`; whole-transaction live-cell/payload and Python process
costs are not claimed complete.

Strict ceiling: two formula-generated bounded circuit families at declared
depths through 64, one fixed `F5` port, and direct-process logical custody. The
result does not establish bounded-width exact state, a distinct phase resource,
computational advantage, general tensor-network contraction, CATVM-enforced
custody, Small Wall crossing, physical waveform execution, physical-bit
replacement, catalytic inference, or unbounded computation.

Reviewed source hashes:

```text
production           82b0eb146d5e79f4d202049cd9c173fcdb9986e35817c831d57034e47a1d0f2e
standalone reference f247a9d1dc58bb5a79d5c57532522dd6a7eb896b4f89a74927a4f8e3208bb2ca
qualifier            53e80d9a36e4f10ffe15ddc30e562d6d845ae98e8f363132845f429f9ef2edec
sealed result        79d6dc9c69fa8531a45886401196f4fd240e5ea9631bb00ba7db28794ae1d89e
sealed reference     a1bdaf4141ac908cfda09c011b99b720cc0adc4c8a5437d6314d75892926c43e
```
