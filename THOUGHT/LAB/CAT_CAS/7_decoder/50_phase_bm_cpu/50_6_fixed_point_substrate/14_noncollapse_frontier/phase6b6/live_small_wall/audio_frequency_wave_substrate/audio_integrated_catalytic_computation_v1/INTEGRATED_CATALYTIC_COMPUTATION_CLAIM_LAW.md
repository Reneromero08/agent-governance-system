# Integrated Catalytic Computation Claim Law

**Status:** `FROZEN_R4S_SOFTWARE_CLAIM_CEILING`

The package may emit:

```text
CATALYTIC_WAVEFORM_ISING_COMPUTATION_VERIFIED
```

only when all frozen evidence reproduces and one focused independent adversarial review
passes with no material finding:

```text
J and h act through complete 5 x 256 complex waveform interactions
the native trajectory is materially non-rank-one across samples
changing carrier content materially changes native history
recursive geometry supplies pointwise complex pair channels
replacement geometries receive their own mean-only calibration
self-calibrated flat and parent-child-scrambled geometries fail and change raw output
native evolution has no decoded-spin, energy, answer, winner, or oracle feedback
the final-only projection agrees with the independent bounded oracle
canonical carrier displacement is nonzero
the external result latch survives reversal unchanged
the exact inverse traversal restores within 2e-12
the restored carrier is used directly for a different computation
the second result differs, agrees with its oracle, and restores within 2e-12
removed transform, removed operator, no-lock, and wrong-query controls fail
wrong, omitted-step, and omitted inverse controls remain outside 1e-3
the scalar J@s baseline is structurally rejected
```

Claim ceiling:

```text
BOUNDED_SOFTWARE_CARRIER_CAUSAL_CATALYTIC_ISING_REFERENCE_ONLY
```

The result does not establish a general or scalable Ising solver; speed, energy,
complexity, or capacity advantage; absence of ordinary software bits; physical audio,
electrical, acoustic, oscillator, or silicon computation; physical carrier persistence
or restoration; hardware bit replacement; or a Wall crossing.

The narrower predecessor results remain valid regardless of this package outcome:

```text
SOFTWARE_CATALYTIC_WAVEFORM_COMPUTATION_VERIFIED
AUDIO_RECURSIVE_CATALYTIC_ISING_EMULATOR_ESTABLISHED
```
