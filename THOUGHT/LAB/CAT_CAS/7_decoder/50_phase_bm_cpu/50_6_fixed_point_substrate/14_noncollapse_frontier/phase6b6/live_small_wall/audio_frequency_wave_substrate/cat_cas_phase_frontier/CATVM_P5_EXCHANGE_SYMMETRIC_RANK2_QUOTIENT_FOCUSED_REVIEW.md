# M245 focused review

Disposition: `PASS_STRICT_SCOPE`

Classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `SEPARATE_REFERENCE_PARITY`

Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

## Verified scope

The accepted CATVM path carries one actual 15-cell `Q(zeta_5)` message indexed
by unordered pairs in `F5^2`.  The exchange-symmetric two-rail transfer and its
inverse use the correct ordered-orbit multiplicities.  The controller receives
only the selected final orbit amplitude, after exact reverse execution,
canonical restoration, and generation advancement.  Reuse consumes the same
message, scratch, and descriptor backings at generation two; a fresh carrier
agrees on the distinct reuse program.

The standalone implementation independently reconstructs the cyclotomic
arithmetic, 15-orbit recurrence, full labelled 25-state recurrence, and the
endpoint-specialized factorized classical boundary recurrence.  It imports no
production, controller, or M237 arithmetic code.  The full labelled recurrence
and the orbit quotient agree at every stage for all three sealed transactions.

Over both split primes 41 and 61, the declared symmetric prefix/suffix language
has reachability, observability, and Hankel rank 15.  This rejects transferable
linear realizations below 15 only for that declared language.  The first rung
has labelled matrix rank 5, rejecting product and matrix-bond ranks below 5.
The separately persisted three-gate exchange-broken labelled alphabet `A/B/D`
has reachability and observability rank 25.  That finite certificate is not a
claim that arbitrary exchange breaking always forces rank 25.

The accepted forward uses 1,140 character terms and its exact inverse uses
1,500.  It retains 15 message and 15 scratch field cells.  The strongest
implemented selected-boundary classical comparator reuses a 15-cell orbit
backing plus 25 temporary cells and uses 440 forward-only character terms.
The identical direct orbit comparator uses 30 message-plus-scratch cells and
1,140 forward terms.  These are a time-memory tradeoff, not computational
advantage.

## Adversarial controls

The package exercises disconnect, partial-forward exception, post-projection
exception, stale generation, wrong owner/type/program/output/consumer/depth,
same-identity descriptor mutation, dirty scratch, missing inverse, a completed
wrong-inverse word, reordered inverse, wrong off-diagonal orbit multiplicity,
zero-rung disconnection, exchange-breaking descriptor rejection, forbidden
intermediate projections, null carrier, snapshot commands, and no-reload
restored reuse.  Failure paths restore before rejection or lost response.

The controller imports no backend or exact-field module.  The service is
nondumpable, closes configuration stdin, serializes no resident message,
scratch, labelled state, path assignment, or transfer matrix, and returns no
secret-dependent intermediate payload metric.  All five source dependencies
are SHA-256 pinned by the combined seal.

## Review repairs

The focused review required and rechecked four narrow repairs:

1. Startup descriptor conversion plus nonzero-validation reads are 168 across
   six configurations, not 102.
2. The factorized comparator now reuses the dead 15-cell input backing after
   its 25-cell first-axis transform, so its implemented peak is 40 field cells.
3. The specialized first forward contributes 15 character terms but no field
   multiplication or accumulation; complete-transaction counts are therefore
   2,625 for each operation class, versus 2,640 character terms.
4. The rank-25 statement is restricted to the exact persisted `A/B/D`
   exchange-broken alphabet rather than generalized to exchange breaking.

Post-repair independent disposition: `PASS_STRICT_SCOPE`.

## Claim ceiling

This is a bounded exact software result for one four-module,
exchange-symmetric, two-rail family behind an abstract Unix-domain service.  It
does not establish an arbitrary nonlinear encoding lower bound, arbitrary
graph closure, fixed bounded-width exact state, a distinct phase resource,
computational advantage, Small Wall crossing, physical waveform execution,
replacement of physical bits, general inference, or unbounded catalytic
computation.
