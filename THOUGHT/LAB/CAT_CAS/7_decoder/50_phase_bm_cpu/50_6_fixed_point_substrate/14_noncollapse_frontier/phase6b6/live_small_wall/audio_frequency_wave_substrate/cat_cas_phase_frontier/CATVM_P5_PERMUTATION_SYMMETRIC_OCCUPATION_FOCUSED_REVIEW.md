# M246 focused review

Disposition: `PASS_STRICT_SCOPE`

Classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `SEPARATE_REFERENCE_PARITY`

Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

## Verified scope

The accepted CATVM path carries exact `Q(zeta_5)` occupation amplitudes for
permutation-symmetric rail counts 2, 3, 4, and 6 at public depth three.  Their
occupation dimensions are respectively 15, 35, 70, and 210.  Every transfer
row is rematerialized from the public multinomial coefficient law into one
fixed row backing; the service retains no labelled `5^n` assignment expansion
and no dense occupation transfer matrix.

The independently implemented reference uses its own cyclotomic arithmetic
and iterative coefficient dynamic program.  It agrees with the service on all
accepted boundaries, and its verifier-only labelled recurrences agree with the
occupation quotient at 25 and 125 labelled states for rails 2 and 3.  The
inverse uses the distinct output-orbit multiplicity law rather than treating
the forward quotient as an unweighted transpose.  Odd- and even-rail
normalization are exact.

At both split primes 41 and 61, the exact forward descriptor family has rank
progressions 15/15/15, 29/35/35, 47/70/70, and 60/205/210 at depths one, two,
and three.  The seal persists exactly `D_n` independent, valid, three-forward-
module public programs for each certificate.  It uses no inverse gates.  All
public final occupation selectors give observable rank `D_n`, so the resulting
finite Hankel certificates have ranks 15, 35, 70, and 210.  This rejects a
smaller transferable linear realization only for the declared finite public
language; it is not an all-`n` or nonlinear lower bound.

The controller receives only the selected final occupation amplitude after
the actual inverse, canonical restoration, and generation advancement.  The
rails-6 reuse transaction consumes the same message, output-scratch,
coefficient-row, and descriptor backings at generation two; a fresh carrier
agrees on the descriptor-distinct reuse program.  No snapshot or baseline
reload is used.

## Resource and baseline boundary

The accepted carrier uses three fixed `D_n`-cell field backings: message,
output scratch, and coefficient row.  Their totals are 45, 105, 210, and 630
field cells.  Exact coefficient and dot work, descriptor traffic, retained
final state, inverse work, restoration, and reuse are reported, while
whole-process RSS, Python objects, allocator, hashing, socket-kernel, and total
integer-operation costs remain incomplete.

The matched forward-only classical comparator uses the same occupation law
with endpoint specialization and fewer declared coefficient-update-plus-dot
terms.  Those terms are not a complete time comparison.  A stronger exact
symmetric-power/two-mode Krawtchouk factorization is disclosed as unimplemented,
so the package makes neither a classical optimality claim nor a computational
advantage claim.

## Adversarial controls

The package exercises wrong owner/type/program/output/generation, stale
generation, same-identity descriptor mutation, null carrier, dirty scratch and
coefficient row, premature projection, missing inverse, a completed wrong
inverse, reordered inverse, disconnected descriptors, odd/even normalization
mutations, orbit-multiplicity mutations, rail-permutation parity, partial
failure, post-projection failure, disconnect, snapshot-command mismatch, and
fresh/restored reuse.  Failure paths restore before rejection or lost response.

The service is nondumpable, closes configuration stdin, and returns no
resident occupation values, scratch values, coefficient rows, secret-dependent
payload metrics, labelled assignments, or transfer matrices.  The controller
imports no backend or field-arithmetic source.  All accepted dependencies are
SHA-256 pinned by the combined seal.

## Review repair

The first rank evidence used an unrestricted forward-plus-inverse closure and
therefore did not certify the accepted depth-three public transactions.  A
first repair mixed shorter prefixes under different padding maps and measured
only 69/70 and 205/210 at exact depth three.  The final oracle instead builds
independent bases from actual exact-depth-two programs and then independently
selects actual exact-depth-three forward programs.  This exposes the real
rails-6 progression 60 to 205 to 210 and certifies full depth-three rank at both
primes without inverse gates.

Post-repair independent disposition: `PASS_STRICT_SCOPE`.

## Claim ceiling

This is a bounded exact software result for permutation-symmetric depth-three
families at rails 2, 3, 4, and 6 behind an abstract Unix-domain service.  The
occupation rank itself grows as `C(n+4,4)`, and exact arithmetic width is not
bounded.  It does not establish arbitrary labelled compression, a general
nonlinear lower bound, arbitrary topology, a distinct phase resource,
computational advantage, Small Wall crossing, physical waveform execution,
replacement of physical bits, general inference, or unbounded catalytic
computation.
