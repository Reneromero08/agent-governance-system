# M243 focused adversarial review

Decision: `PASS_STRICT_SCOPE`

Verification classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `SEPARATE_REFERENCE_PARITY`

Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

## Reviewed scope

The review inspected the production service, public controller, standalone
power-basis oracle, qualifier, and regenerated sanitized evidence. It checked:

- the exact p=5 quadratic Gauss completion with one cubic linear functional;
- the independent dense sum for every declared case through `n=4`;
- the independent modular elimination path at every declared width;
- in-place no-pivot `LDL^T` forward and exact reverse reconstruction;
- atomic response ordering, exception rollback, disconnect restoration, and
  generation-correct same-backing reuse;
- controller exclusion, hidden descriptor and quotient custody, projection
  rejection, and absence of secret-dependent durable receipts;
- the identical compact LDL-plus-five-term classical baseline;
- logical, exact-payload, protocol, verification, and runtime caveats; and
- the strict negative claim ceiling.

## Repairs required by review

The first source pass found that the claimed connected matrix family was
validated only by an off-diagonal edge count. The accepted source now builds
the actual support graph, requires a complete traversal, independently checks
that property, and rejects a regular disconnected diagonal descriptor.

The second pass found that a one-cell final field boundary could be mistaken
for fixed-width exact state. The accepted evidence now denies fixed-width
state and records public numerator and denominator material bounds growing
with `n`. The retained denominator metadata is correctly named as one scalar
cell rather than an exponent value.

The accepted source also counts the hidden descriptor retained by the CATVM
configuration separately from the mutable carrier, counts both forward and
inverse descriptor reads, identifies the two hidden quotient residue scratch
cells, and counts Q(zeta5) coordinate and denominator metadata.

## Accepted result

Connectedness is now enforced by support-graph traversal. Production and the
independent power-basis implementation agree on the exact five-channel formula
at widths `2,3,4,6,8,12,16`; independent dense enumeration agrees only in its
declared verifier role through `n=4`. The production no-pivot `LDL^T` chart and
the independently pivoted modular elimination reach the same final amplitude.

The actual packed matrix backing is transformed and reconstructed in place.
The solve and five-channel scratch are cleared, the hidden descriptor is
subtracted, exact canonical state is verified, and only then is the final
amplitude response released. Partial-prefix failure, post-projection failure,
and disconnect all restore before rejection or response. The same backings
advance from generation one to generation two and agree with a fresh execution
of the reuse descriptor.

The result is a bounded quotient collapse, not compute leverage. One cubic
direction reduces exactly to five coherent channels determined by dimension,
determinant square class, `u^T A^-1 u`, and cubic strength. The strongest
implemented classical comparator is the identical compact modular elimination
and five-term closure. Exact payload width still grows under normalization.

No standard one-shot quantum inference, rank-one escape from the fixed Gauss
quotient, general nonlinear phase resource, total computational advantage,
Small Wall crossing, physical waveform execution, physical-bit replacement,
general inference, or unbounded catalytic computation is established.
