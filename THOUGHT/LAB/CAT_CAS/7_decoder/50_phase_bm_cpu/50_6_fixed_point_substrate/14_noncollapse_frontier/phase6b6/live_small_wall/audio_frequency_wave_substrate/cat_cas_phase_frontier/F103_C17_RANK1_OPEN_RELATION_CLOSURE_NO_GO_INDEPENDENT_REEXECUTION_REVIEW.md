# F103 C17 Rank-One Open-Relation Closure Independent Reexecution Review

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE` at
`INDEPENDENT_ORACLE_REEXECUTION`.

Resident restoration is `EXACT_ALGEBRAIC_RESTORATION`; temporary buffers have
`NO_RESTORATION_CLAIM`.

## Verified mechanism

M153 represents each non-translation-invariant relation as
`R(x,y)=a[x]b[y]`. A shared resident control `C=u v^T` acts on eight target
relations by left composition with `I+C` and parallel Hadamard intersection
with `C`. Both actions preserve rank one and are exactly invertible on the
declared nonzero-control family. Composition and intersection order changes
the final boundary.

The scalar-Python oracle imports neither production nor NumPy. It reconstructs
18 public cases through depth 512, all 612-byte commitments, the complete
target-factor recurrence, final boundaries, inverse attacks, streamed
relation semantics, and a rematerialized-control classical recurrence. All
414 case-field comparisons pass.

No 17 by 17 relation table or assignment expansion is retained. The semantic
oracle streams 289 scalar entries for each accepted relation law. A separate
two-term control perturbation has an explicit nonzero 2 by 2 minor (value 34
at rows 0,1 and columns 0,1), proving exact rank two without materializing the
dense relation.

## Restoration, resources, and ceiling

The same 612-byte carrier backing restores exactly with no snapshot, inverse
history, retained baseline, or canonicalization. Unrelated depth-311 reuse
matches fresh execution after depth 37, and 64 depth-8 reuse cycles restore.
Direct-process type, owner, and generation checks are not machine custody.

The strongest executed baseline retains only the 306 target-factor
coordinates and rematerializes the immutable 34-coordinate hub control. Its
target state and boundary match every case. The phase carrier therefore uses
twice the resident coordinates, while both paths count 278,528 relation field
multiplications at depth 512. No optimal classical recurrence is claimed.

The result is limited to rank-one `C17 -> C17` relations, identity-plus-rank-
one composition, rank-one intersection, the declared nine-control/nine-target
topology, and Linux direct-process software. It does not establish rank-two
or general relation closure, CATVM custody, a distinct phase resource,
computational advantage, a Small Wall crossing, physical execution, physical
bit replacement, or unbounded catalytic computation.
