# M254 focused independent review

Disposition: `PASS_STRICT_SCOPE`

Scientific source commit: `aad46509f9760a5e03998c2405f334e2e9ff28d6`

Verification classification: `INDEPENDENTLY_VERIFIED_STRICT_SCOPE`
Verification level: `SEPARATE_REFERENCE_PARITY`
Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

The review independently inspected the production service, public controller,
standalone polynomial-quotient oracle, qualifier, and all three sealed JSON
artifacts. The sealed dependency hashes match the scientific source exactly.

The signed exterior multiplication law and the eight-variable Berezin-kernel
derivation agree on all eight even basis masks. The resulting signed-complement
Hodge map is an exact involution and restricts to M253's Gaussian Pfaffian
transform. Public intersection factors with nonzero scalar have the exact
finite nilpotent inverse `s^-1(1-n+n^2)`; the accepted factor family requires
the `n^2` term. The independent quartic coordinate violates the Gaussian
pure-spinor identity and changes the selected boundary.

The separate oracle reconstructs `Q[zeta8]/(zeta8^4+1)`, generic bitmask
exterior multiplication, the Berezin kernel, factor inversion, and an
independent custody state machine without importing production. It reproduces
all accepted boundaries, work records, exact restoration, generation-two
reuse, and no-reload behavior. Its declared public prefix/suffix family has
exact reachable, observable, and Hankel ranks eight. That certificate applies
only to the declared linear language and does not exclude nonlinear charts.

The service retains one actual eight-cell relation backing, one Hodge scratch
cell, and eight receipt cells. Only the final top-form coefficient is released,
and only after reverse execution, exact canonical verification, and release.
Disconnect, partial-failure, post-projection-failure, missing/wrong/reordered
inverse, descriptor mutation, stale generation, premature projection, and
forbidden protocol controls discriminate. No hidden coefficient vector,
scratch value, factor inverse, truth table, assignment list, or path expansion
appears in the controller response.

The strongest implemented transferable comparator is the identical in-place
eight-cell exterior recurrence with one reusable scratch cell and no CATVM
restoration transaction. The full even interface dimension is `2^(m-1)`.
Whole-process liveness, exact coordinate payload, public factor containers,
Python objects, allocator state, socket/hash serialization, timing, and RSS
remain excluded or unclaimed. No distinct phase resource, computational
advantage, general Grassmann closure, Small Wall crossing, physical execution,
physical-bit replacement, inference, or unbounded computation is established.
