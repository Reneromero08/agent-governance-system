# Claims Under Test

Source generation:
`c0cee6a9475d35bc64c90ec30567826bcf3c9e9a`.

Every statement below begins as:

```text
CLAIMED_BY_SOURCE_BRANCH
NOT_YET_REPRODUCED
NOT_YET_TRANSFER_VERIFIED
NOT_CANONICAL
```

## Candidate A

Whether native `F` produces a genuinely hidden resident `Y`, native `G`
consumes that exact `Y`, only final `Z` is projected, actual `G^-1` then
`F^-1` restores the same carrier, and that carrier can be reused without
snapshot reload, public-input regeneration, controller recomputation, or
answer leakage.

The source currently emits the boundary before restoration. That ordering is
explicitly not accepted as the requested atomic response-after-restoration
law and must be attacked or repaired in any transfer experiment.

## Candidate B

Whether the source automatically derives a reversible schedule for its
declared 15-node shared DAG, maintains generation and edge custody, performs
literal inverse/rematerialization, restores its allocator and carrier, and
responds only after restoration without controller access to intermediates.

The exact-fixture specialization to topology ID 813 and the 28/28/9 schedule
are under test; they are not assumed to generalize.

## Candidate C

Whether, under matched boundaries and complete accounting, a strongest
compact conventional evaluator dominates snapshot-backed and in-place CATVM
arms for the full fixed public QANF schema. The four source fixtures and its
chosen four-AND baseline are not accepted as exhaustive proof.

## Candidate D

Whether future-continuation equivalence yields exact suffix quotient classes
for the declared bounded families, whether the quotient is neither too coarse
nor unnecessarily fine, and whether any mechanism transfers beyond
homogeneous neighbor-AND/OR chains.

The tracked result is not bound to this frozen source hash. The source
reference repeats the production class formula, so parity alone is not an
independent derivation.

## Explicit nonclaims

- No result is canonical.
- The Small Wall is not crossed.
- No physical Family 10h evidence is changed or reinterpreted.
- No software timing, F3 arithmetic, or phase-carrier threshold transfers to
  the physical CPU.
- Source result JSON, review JSON, branch state, roadmap prose, and commit
  messages are not independent evidence.
