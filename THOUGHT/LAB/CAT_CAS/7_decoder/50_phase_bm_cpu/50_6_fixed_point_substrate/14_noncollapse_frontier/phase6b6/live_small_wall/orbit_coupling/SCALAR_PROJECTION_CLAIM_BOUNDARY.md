# Scalar Projection Claim Boundary

Status: `FROZEN_CLAIM_BOUNDARY_CORRECTION`

Package retired: `orbitstate_gain_covariant_confirmation_v1_0`

Retirement receipt:

```text
orbitstate_gain_covariant_confirmation_v1/GAIN_COVARIANT_CONFIRMATION_V1_RETIREMENT.json
```

## Correct Retained Interpretation

The historical official class remains:

```text
ORBITSTATE_INDEPENDENT_COUPLING_CANDIDATE
```

The retrospective gain-covariant geometry is preserved only under this corrected
claim ceiling:

```text
GAIN_COVARIANT_ORBITSTATE_PROJECTION_TRANSDUCTION_ESTABLISHED
```

Do not promote or infer:

```text
PRIVATE_ORBITSTATE_ACCESSOR_ESTABLISHED
CATALYTIC_BORROWING_ESTABLISHED
PHYSICAL_RELATIONAL_MEMORY_ESTABLISHED
SMALL_WALL_CROSSED
```

## Source-Side Scalar Projection

The retired package gave the source process enough private information to compute the
claimed OrbitState projection before physical transmission:

1. The private source record contained state, phase, response mode, and polarity.
2. The source computed scalar `q_theta`.
3. The source physically transmitted same-total work as `M + q_theta` versus
   `M - q_theta`.
4. A receiver-side Change-to-Dirty decoder measured the physical carrier response.

For `d = 23`, `N = 256`, and `phi = 2*pi*23/256`, the source-authored ideal vectors
are real geometric objects:

```text
Q_d        = 1536 * exp(+i*phi)
Q_fold     = 1536 * exp(-i*phi)
Q_polarity = -1536 * exp(+i*phi)
```

The gain-covariant audit shows these vectors transported through a stable positive
carrier gain. That is a real projection-transduction result.

## Ordinary Scalar Explanation

The same result follows from an ordinary odd-linear carrier:

```text
F(q) = g q
```

The source writes the two physical legs:

```text
M + q_theta
M - q_theta
```

The physical difference is:

```text
(M + q_theta) - (M - q_theta) = 2 q_theta
```

Therefore a positive scalar PMU channel reproduces:

```text
target phase
fold conjugacy
polarity inversion
odd magnitude
```

without hosting an unresolved relation and without reading a hidden branch from the
carrier.

## Claim Ceiling

Gain calibration improves measurement of the scalar transducer. It does not create
an accessor. It estimates the nuisance carrier gain after the source has already
authored the scalar projection.

Byte restoration is `R0`, not physical carrier restoration. It proves bytes or hashes
return. It does not prove that a physical coherence, PMU, path, or observable carrier
state returns to an accepted equivalence class.

A Small Wall experiment must instead preserve the unresolved relation through
physical preparation or evolution and delay the receiver query or collapse boundary
until after source preparation closes.

## Replacement Requirement

The successor architecture must enforce this causal chain:

```text
unresolved relational object prepares the carrier
source preparation closes
receiver chooses the query
carrier produces response without source participation
receiver extracts a predeclared relation
physical restoration closes
```

If scalar replay from source-visible inputs and source receipts reconstructs the
claimed invariant, the design is invalid for Small Wall evidence.
