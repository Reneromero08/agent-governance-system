# M130 Independent Review: Rank-Two Coherent Secant Closure

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Resident rank-two coherent phase-carrier restoration:
`EXACT_ALGEBRAIC_RESTORATION`

Transient inverse-coupling, projection, commitment, compiler,
full-occupation-oracle, and classical-baseline buffers:
`NO_RESTORATION_CLAIM`

The verified scientific source is commit
`e728386bef818bf92a497cf6ff1f183971e36e8c`. M130 is a bounded nonlinear
successor to M129. It preserves the M128 linear irreducibility result and the
M129 rank-one chart ceiling.

## Verified mechanism

Let `R` swap modes zero and one in the degree-`k` symmetric sector. The
accepted coupling is `C_eta = I + eta R`, where `eta` is the declared nonzero
power of `zeta17`. Because `R^2 = I`, the exact inverse is

```text
C_eta^-1 = (I - eta R) / (1 - eta^2).
```

The rank-one seed `x0^k` becomes the rank-two coherent secant
`x0^k + eta*x1^k`. An independent first-derivative minor is nonzero in every
declared algebra. Two fixed, boundary-noncommuting 20-primitive modules then
consume the actual two-component resident state. Only the final
`(k-1,1,0,...,0)` occupation scalar is projected.

Exact `Q(zeta17)` transactions pass at `k=4,8,16,32`; structural `F103/F137`
transactions pass through `k=128`. The accepted resident representation has
36 field cells: two scalar weights and two 17-coordinate coherent vectors.
The declared inverse coupling forms at most four component records, or 72
logical field cells, before exact duplicate cancellation. Python container,
allocator, native-library, bigint, and whole-process peaks remain excluded.

Actual reverse module execution followed by the exact coupling inverse
restores the seed, unloads it to exact zero on the same backing, advances the
generation, and supports unrelated `PRIMARY k=8` then `REUSE k=16` execution.
Fresh and restored boundaries and the complete reported resource signatures
agree without snapshot reload or retained inverse history.

## Independent reconstruction and repair

The oracle does not import the M130 production module. It separately compiles
the public word, reconstructs the two-component recurrence, coupling,
boundary, inverse, duplicate merge, and strongest compiled endpoint baseline.
It expands all `H(4)=4,845` occupation coordinates in `Q(zeta17)`, `F103`, and
`F137`; the full recurrence, boundary, and exact inverse agree in all three
algebras. All 16 declared transaction commitments and boundaries agree.

The first review rejected two overstatements. First, four pairwise-distinct
generated terms after a second coupling do not prove minimal secant rank four.
The repaired package reports only four distinct generated terms and explicitly
leaves the minimal rank and repeated-coupling rank law unestablished. Second,
the two public weights can be folded into the mode-one endpoints. The repaired
strongest fixed-word warm baseline therefore retains exactly four total
scalars, not six. Production, oracle, seals, and qualifier independently gate
the folded commitment, four-cell retention, and boundary parity.

Missing, wrong, and reordered inverses; premature projection; wrong ownership;
null carrier; missing exact merge; coupling omission; module reordering; and
rank-one misuse are detected. No accepted-path occupation vector, occupation
topology, matching/assignment expansion, dense operator, intermediate secant
payload, relation table, or inverse history is materialized.

## Matched baseline and claim ceiling

For descriptor-runtime execution, compact classical software uses the
identical two-component 36-cell recurrence. For each sealed word, the stronger
warm baseline compiles through 36 cells and retains four total folded endpoint
scalars. No computational advantage is claimed.

The strict ceiling is one involutive superposition coupling from the declared
rank-one seed, two fixed 20-primitive consumer modules, exact `Q(zeta17)` at
`k=4..32`, dual-field structural parity at `k=4..128`, and direct-process
software execution. Exact payload-height maxima remain package-local; the
independent verification covers algebra, topology, boundaries, commitments,
logical 36/72-cell accounting, restoration, reuse, and matched baselines.

M130 does not establish a repeated-coupling rank law, M127 grid closure,
arbitrary secant input closure, Gaussian closure, CATVM custody, a distinct
phase resource, computational advantage, Small Wall crossing, physical
waveform execution, replacement of physical bits with pi, or unbounded
catalytic computation.

The next obstruction is that a second coupling produces four distinct
generated terms, but no minimal-rank lower bound above two has yet been proved.
The next experiment must derive an exact catalecticant lower bound or a
genuinely closed Gaussian chart, retaining the strongest compact classical
baseline without assignment enumeration.
