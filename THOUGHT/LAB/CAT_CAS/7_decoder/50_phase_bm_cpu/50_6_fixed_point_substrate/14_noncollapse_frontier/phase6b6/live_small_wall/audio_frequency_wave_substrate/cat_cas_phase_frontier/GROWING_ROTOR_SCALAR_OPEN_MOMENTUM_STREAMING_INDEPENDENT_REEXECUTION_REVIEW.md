# Independent reexecution review: Rotor-6 scalar open-momentum stream

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

Result: `PASS_COMPACT_PORT_STREAMING_WITH_TOPOLOGY_WORK_TRADEOFF`

For the declared F103 Rotor-6 fixture, one resident owner-typed scalar port is
sufficient to compute, consume, and clear each necklace intermediate. The
accepted path retains neither M199's 4,389-cell open-momentum vector nor M201's
74,613-cell occupation scratch. It also retains no transition plan, dense
2,277-by-2,277 operator, or permanent expansion.

This is a bounded storage repair, not evidence of a distinct phase resource.
The same recurrence has an identical one-cell classical implementation, and
the compact path pays substantial public-topology rematerialization work.

## Production mechanism

The production path preserves M199's exact identity

```text
K_q = A_-q A_q - rotor_count I
```

but changes the custody schedule. For each paired momentum and necklace
coordinate it:

1. leases one scalar field cell with momentum type, generation, and necklace
   owner;
2. computes that coordinate directly from the resident 2,277-cell bracelet
   carrier;
3. rematerializes the public rotation/reflection fanout that consumes the
   scalar;
4. scatters only to exact bracelet targets;
5. clears and releases the same scalar backing before advancing.

The port is never projected while live. Production controls reject wrong type,
wrong owner, premature projection, null carrier, and the missing, wrong,
reordered, and wrong-reflection inverse cases.

## Independent construction

The oracle imports neither the production scalar-stream module nor M199
production. It reuses the earlier separate-reference topology, independently
transposes verification-only one-body rows, and reconstructs the scalar
stream, direct two-body CSR, factor result, boundary, restoration, and resource
law.

The oracle independently reconstructs its own owner-typed scalar port and
checks wrong-type, wrong-owner, and premature-projection rejection. It holds
the same one-cell port backing through primary execution, inverse
rematerialization, unrelated restored-carrier reuse, and fresh reuse.

The independently retained 494,496 plan entries are verification-only oracle
state. They are excluded from the accepted production path and explicitly
reported rather than attributed to the one-cell mechanism.

Production and oracle agree exactly on:

- topology commitment;
- primary state commitment;
- primary boundary 83;
- restored-carrier and fresh reuse boundary 70;
- zero direct/factor mismatch;
- 6,832 active numeric cells;
- 105,832 named field-and-descriptor slots;
- 5,534,928 inverse fanout candidates; and
- 331,704 accepted one-body contributions.

## Restoration and controls

Primary, reuse, and fresh-reuse transactions restore the actual source and
target backings exactly over F103. The resident scalar port is cleared on the
same backing after every lease. Reuse advances the carrier restoration
generation to two and agrees with fresh execution without snapshot or baseline
reload.

Missing, wrong-program, reordered-noncommuting, and wrong-reflection controls
leave 2,257, 2,252, 2,253, and 2,254 mismatched field cells respectively.

## Resource law and matched baseline

The accepted active numeric state is:

```text
2,277 source + 2,277 target + 2,277 output + 1 scalar = 6,832 field cells
```

Public topology contributes 96,723 predecessor descriptor integers and the
exact bracelet lookup contributes 2,277 indices. Total named algorithm state
is therefore 105,832 logical field-and-descriptor slots, before Python
containers, big integers, allocator/interpreter overhead, timing, native
library memory, or whole-process peaks.

This is only 2,111 slots below the comparable M199 accounting of 107,943.
One forward scattering rematerializes 1,193,808 source-orbit rotations and
5,534,928 inverse move candidates to accept 331,704 one-body contributions.
M199 retains a 4,389-cell port and avoids this exact fanout reconstruction.

The strongest compact classical baseline is the identical one-cell
topology-rematerialized recurrence. Consequently, the storage reduction
cannot be claimed as computational advantage or as a phase-only capability.

## Claim ceiling

The result is limited to grid 17, six exchange-symmetric rotors, the declared
rotation- and reflection-invariant sector, F103 root 72, depth-one primary and
reuse programs, direct-process software, and this scalar streamed factor law.

It does not establish CATVM custody, a general open-port algebra, compact
growth across increasing rotor count or depth, a distinct phase resource,
computational advantage, a Small Wall crossing, physical waveform execution,
replacement of physical bits with pi, catalytic inference, or unbounded
catalytic computation. The next obstruction is to reduce the public-topology
fanout/rematerialization law without recreating a wide intermediate or merely
encoding the same compact classical recurrence.
