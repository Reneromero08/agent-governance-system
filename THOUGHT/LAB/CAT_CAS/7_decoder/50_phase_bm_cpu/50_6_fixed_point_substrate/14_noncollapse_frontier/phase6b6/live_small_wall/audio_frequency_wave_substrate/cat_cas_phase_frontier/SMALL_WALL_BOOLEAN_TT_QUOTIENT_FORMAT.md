# Growing Boolean-TT Quotient Small-Wall Triad

## Status

This bounded diagnostic establishes:

```text
GROWING_NONAFFINE_BOOLEAN_TT_SUFFIX_QUOTIENT_SMALL_WALL_TRIAD_CONFIRMS_COMPACT_RECURRENCE_AND_RETAINED_HISTORY_OBSTRUCTION
```

within:

```text
BOUNDED_LINUX_WARM_WIDTHS4_5_8_12_16_DEPTHS4_5_8_HOMOGENEOUS_NEIGHBOR_AND_OR_REFERENCE_ONLY
```

It does not establish computational advantage, a Small Wall crossing,
fixed-rank unbounded-depth closure, general Boolean-TT minimization, physical
phase execution, or unlimited catalytic computation.

## Matched arms

Each arm is a persistent private `AF_UNIX/SOCK_SEQPACKET` service with the
same public width, depth, AND/OR family schedule, final receipt, request
packets, response packets, and protocol bytes:

```text
direct compact baseline
    stream every public threshold-quotient cell in O(final cells)
    no cache, answer table, carrier, raw product, or phase engine

snapshot sham
    execute the real phase forward chain and final projection
    restore working state from the comparison snapshot

in-place CATVM phase path
    execute the same phase forward chain and projection
    apply the actual boundary, stage, and leaf inverses
    reuse the same restored carrier
```

Only the final boundary cell count, one-count, rank-plan hash, and boundary
hash cross the service boundary. Intermediate stages cannot be projected.
The controller contains no quotient generator or expected result material.

The timed baseline is the strongest compact family-specific construction,
not the exponential raw-product verifier. The verifier is run separately and
its raw TT materialization is disclosed.

## Diagnostic result

Five growing cases use 32 warm and 128 timed transactions per observation in
the fixed arm order:

```text
baseline, snapshot, in_place, in_place, snapshot, baseline
```

At width 16/depth 8:

```text
final quotient cells                         3,548
retained stages                             13,740 cells
predecessor inverse history                 10,192 cells
total phase carrier                         17,288 cells
live carrier / sealed state / comparison       553,216 bytes each
snapshot working reload                        276,608 bytes/transaction
baseline carrier-seal CPU                         2,484 ns
snapshot carrier-seal CPU                       936,436 ns
in-place carrier-seal CPU                       898,906 ns
baseline -> snapshot service CPU ratio          133.686x
baseline -> in-place service CPU ratio           254.029x
```

The ratios are bounded warm software observations, not a general performance
law. The structural adjudication does not depend on timing: this homogeneous
family exposes a public threshold recurrence that directly generates the
same quotient core. No computational resource distinct from that compact
Boolean recurrence is present in the current root-locked phase law.

The retained inverse history is a separate phase-machine defect. It adds a
depth factor to carrier storage and is therefore selected for an immediate
phase-owned reversible-rematerialization repair.

## Reproduction

```bash
evidence_parent=$(mktemp -d /tmp/boolean-tt-small-wall.XXXXXX)
bash qualify_small_wall_boolean_tt_quotient.sh \
    "$evidence_parent/evidence"
```

Reviewed local evidence:

```text
/tmp/boolean-tt-small-wall.TNqwzG/evidence
```
