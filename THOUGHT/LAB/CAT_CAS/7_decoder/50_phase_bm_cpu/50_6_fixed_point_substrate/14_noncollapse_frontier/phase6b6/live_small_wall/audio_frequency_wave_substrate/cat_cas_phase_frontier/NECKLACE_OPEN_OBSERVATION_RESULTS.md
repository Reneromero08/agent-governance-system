# Necklace Open-Observation Relation Closure

## Result

Six typed public observation ports now close natively into the unresolved
285-cell necklace phase carrier. The bounded claim is:

```text
BOUNDED_COHERENCE_DEPENDENT_TYPED_OPEN_OBSERVATION_RELATION_CLOSURE_AND_CATALYTIC_HYPOTHESIS_SCORING_ON_COMPACT_NECKLACE_PHASE_CARRIER_WITH_ACTUAL_RESTORATION_AND_REUSE
```

Final evidence:

```text
/tmp/four-rotor-necklace-open-observation-final-v2
```

The evidence manifest binds the exact source dependency chain, qualifier,
executed binary, and result. A separate ASan/UBSan build passes the same
transaction with empty stderr.

Focused review `FOUR-ROTOR-NECKLACE-OPEN-OBSERVATION-01` returned `PASS` with
no remaining findings. It explicitly limits the result to typed public
observation substitution closure, not shared unresolved internal-port
composition or general open relational computation.

The ceiling is direct-process, exchange-symmetric, rotation-invariant, grid
17, four rotors, six typed public observations, complex128, and software only.
There is no enforced machine boundary in this experiment.

## Open relation and native closure

Before closure, each public signature is a phase-valued relation between an
unresolved necklace state `x` and an open typed observation port `o`:

```text
R(x, o) = omega^(strength * (feature(x) - o)^2)
```

The two relation types are:

- collision count, with observation domain `0..6`;
- cyclic-separation pair count, with observation domain `0..4`.

The six public observations are `1, 2, 0, 3, 1, 4`. Native substitution
closes each observation port directly into the phase relation. The accepted
path does not enumerate either observation domain, materialize a relation
table, project the 285 latent necklace amplitudes, or expand truth-table or
candidate-assignment cells.

Each closed relation is followed by a noncommuting Hermitian necklace
generator update. Six modules therefore compose through the unresolved
carrier before the only surviving boundary is projected.

## Bounded hypothesis-scoring contract

The final hypothesis variable is total unordered occupation-collision count,
with domain `0..6`. The declared score is the weighted collision-class
interference mass, not a calibrated Bayesian posterior. The deterministic
decision is argmax with the lowest index as tie break.

The tested score is:

```text
[0.47745721167016919,
 0.460098045965949,
 0.01934703306632151,
 0.040188169020947427,
 0,
 0,
 0.0029095402766043921]
```

The selected hypothesis is `0`. An independent hypothesis-by-hypothesis
aggregation of the final resident carrier agrees exactly with the projected
boundary.

This establishes a bounded catalytic hypothesis-scoring contract. It does
not establish general catalytic inference, calibrated statistical inference,
ground-truth accuracy, learning, or inference advantage.

## Restoration, reuse, and controls

- primary weighted-norm error: `8.548717289613705e-15`;
- primary actual-inverse restoration error: `9.656765549913727e-15`;
- generation-two restoration error: `1.4051092836756142e-14`;
- fresh/restored reuse boundary error: `8.43769498715119e-15`;
- carrier backing preserved across both transactions.

The final score survives outside inverse history. Missing, wrong, and
applicably reordered inverse controls separate by `1.35255`, `1.01851`, and
`0.980492`. Bypassing one relation changes the boundary by `0.0443233`, and
swapping two noncommuting modules changes it by `0.0498213`.

Initial-and-interstep necklace-basis dephasing changes the boundary by
`0.0390454`. That dephased comparison is outside the accepted coherent path
and streams 487,350 transition coefficients through 198,838,800 permanent
assignment terms. Those terms are disclosed comparison cost, not accepted
path state.

## Resource and claim boundary

The accepted primary lifecycle uses:

- 285 resident complex carrier cells / 4,560 verification-baseline bytes;
- 3,420 relation-phase cell updates across forward and inverse;
- 12 open-observation closures across forward and inverse;
- 768 generator applications and 12,651,264 streamed generator terms;
- 33,638 bytes of explicit engine payload;
- 56 bytes of final projection;
- 38,310 bytes of explicit lifecycle payload;
- zero materialized relation-table cells.

Topology compilation visits 4,845 weak compositions to construct the public
necklace quotient. The accepted runtime emits no intermediate state, but
direct-process execution does not enforce no-smuggle custody.

The best matched compact classical arm is deliberately the identical complex
recurrence and agrees exactly. Therefore the surviving obstruction is:

```text
TYPED_OPEN_OBSERVATION_RELATION_PHASE_CLOSURE_AND_BOUNDED_HYPOTHESIS_SCORING_ESTABLISHED_BUT_IDENTICAL_COMPACT_CLASSICAL_COMPLEX_RECURRENCE_INHERITS_IT
```

No distinct phase resource, computational advantage, Small Wall crossing,
physical execution, or unbounded computation is established.

The next phase-owned mechanism is:

```text
COHERENT_SHARED_LATENT_OBSERVATION_PORT_PHASE_CONTRACTION_ON_NECKLACE_CARRIER
```

It must leave at least one observation interface unresolved across multiple
noncommuting relation modules, close that shared port natively without a
relation table or assignment expansion, preserve restoration and reuse, and
compare against the strongest compact classical contraction rather than an
expanded sham.
