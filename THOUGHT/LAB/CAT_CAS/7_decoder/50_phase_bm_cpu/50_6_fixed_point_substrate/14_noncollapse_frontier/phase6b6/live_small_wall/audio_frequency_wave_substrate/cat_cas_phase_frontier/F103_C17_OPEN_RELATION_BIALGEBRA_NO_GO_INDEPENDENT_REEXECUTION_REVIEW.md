# F103 C17 Open-Relation Bialgebra Independent Reexecution Review

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Restoration class: `EXACT_ALGEBRAIC_RESTORATION` for the resident carrier and
`NO_RESTORATION_CLAIM` for temporary transform and convolution buffers.

## Reconstructed mechanism

The production package encodes a translation-invariant weighted binary open
relation over `F103` by the 17-coordinate signature
`R(x,y) = r[(y-x) mod 17]`.  It composes two signatures through a hidden
interface by cyclic convolution and intersects parallel relations by Hadamard
product.  A public nine-node rotating-hub topology applies compose-then-
intersect and intersect-then-compose triangular shears to one shared resident
port with eight consumers in each layer.

The independent oracle imports neither production code nor NumPy.  It rebuilds
the seed, public descriptors and fingerprints, exact 459-coordinate
coefficient recurrence, 17-mode transform recurrence, final projection,
inverse schedule, resource counters, streamed scalar relation semantics, and
reuse controls using scalar Python arithmetic modulo 103.

## Executed evidence

- 18 exact cases: three public families at depths 1, 4, 16, 64, 256, and 512.
- 360 case-field comparisons against the sealed production result.
- Exact agreement between coefficient and dual-spectral final states on all
  459 coordinates and agreement of all 17 final boundary coordinates.
- 289 streamed scalar checks each for composition and intersection, without
  retaining a 17 by 17 table or assignment expansion.
- Missing, wrong, and reordered inverse attacks; null carrier and null-port
  attacks; wrong type and owner; premature and resident-port projection;
  order, topology, and semantic perturbations.
- Unrelated depth-311 reuse after a depth-37 transaction and 64 repeated
  depth-8 reuse cycles on the same backing carrier.

## Resource law and ceiling

The phase-coefficient and dual-spectral recurrences each retain 459 one-byte
field coordinates.  At depth 512 each performs 2,506,752 counted bialgebra
field multiplications; their equal total reflects the dual exchange of cheap
Hadamard multiplication and expensive cyclic convolution.  The accounting
does not include Python containers, allocator state, NumPy/native internals,
or whole-process peak memory, and it does not claim an optimal compact
classical implementation.

The verified claim is limited to translation-invariant `C17 -> C17` weighted
relations, the declared nine-node three-slot topology, the 18 executed cases,
and Linux direct-process software.  Type, owner, and restoration-generation
checks are direct-process bookkeeping, not machine-enforced lease custody.
The result establishes bounded native
open-relation composition and intersection without dense relation tables, but
translation invariance reduces every relation to 17 classical coefficients.

It does not establish non-translation-invariant relations, arbitrary topology
or arity, CATVM custody, a distinct phase resource, computational advantage,
a Small Wall crossing, physical waveform execution, replacement of physical
bits with pi, or unbounded catalytic computation.
