# CATVM Four-Rotor Incremental Results

Evidence:

```text
/tmp/catvm-four-rotor-incremental-repaired.KpxZnT
```

Bounded claim candidate:

```text
BOUNDED_CATVM_ENFORCED_PROBE_FREE_INCREMENTAL_BESSEL_SCHMIDT_HIDDEN_TT_COMPOSITION_WITH_ACTUAL_INVERSE_CANONICAL_RESTORATION_AND_REUSE
```

The existing four-rotor incremental Schmidt carrier now runs behind a Linux
same-UID `AF_UNIX/SOCK_SEQPACKET` custody boundary. The protocol-only
controller cannot import the NumPy/SciPy phase backend. The service fixes the
two admitted programs, verifies peer credentials, sets its socket mode to
`0600`, disables core dumps and ptrace eligibility, and denies intermediate
projection and null-carrier requests. Ordinary controller and service
stdout/stderr are empty, and a live `/proc/PID/mem` inspection attempt is
denied.

The primary transaction retains its unresolved phase carrier inside the
service, projects only the seven declared final-boundary scalars, performs
the actual inverse on the borrowed carrier, canonically closes it, and then
uses that same restored carrier for an unrelated second program. Restoration
generations advance from one to two. No snapshot is loaded and no inverse
history is retained. The 1,856-byte verification baseline is counted and is
used only after inversion to measure physical distance; it is never assigned
to or reloaded into the accepted carrier.

The matched diagnostic arms use the same public programs, boundary schema,
fixed packet shapes, and 64,512 logical protocol bytes:

```text
arm                 engine ms   path/RTT ms   peak array bytes   couplings
direct process        541.755       541.836          6,870,816           9
isolated sham         549.193       549.954          6,870,816           9
snapshot sham         539.733       540.426          6,872,672           9
in-place CATVM       1549.469      1549.764         10,834,016          18
```

The direct arm's protocol traffic is a declared shape-equivalent accounting
baseline rather than actual IPC. The isolated, snapshot, and in-place arms
each exchange the fixed packets through the socket. Snapshot creation and
service initialization measure the same combined creation interval and must
not be added as independent costs.

All four primary boundaries agree exactly. Restored-carrier reuse agrees
with fresh/direct execution to `6.003e-8`. Missing, wrong, and prospectively
reordered inverse controls separate.

The deterministic obstruction is:

```text
ACTUAL_INVERSE_REQUIRES_2X_NATIVE_COUPLING_APPLICATIONS_IN_THIS_BOUNDED_IMPLEMENTATION
```

This establishes the bounded CATVM machine law for this carrier. It does not
establish seccomp confinement, locked-memory resistance, cross-UID secrecy,
a distinct phase resource, computational advantage, Small Wall crossing,
unbounded computation, or physical waveform execution. The best matched
classical incremental TT executes the same algebra.

