# I1 Retained-Evidence Compatibility Audit

## Result

```text
I1_READ_ONLY_COMPATIBILITY_AUDIT_COMPLETE
H0_PARTIAL_CALIBRATION_ONLY
H1_THROUGH_H7_NOT_PASSED
I2_SYNTHETIC_GENERATOR_CATALOG_ALLOWED
TRANSPORT_CANDIDATE_FREEZE_NOT_ALLOWED
NO_LIVE_EXECUTION_AUTHORIZED
FAMILY10H_HOLONOMY_NOT_ESTABLISHED
NATIVE_CATALYTIC_FIBER_PUSHFORWARD_NOT_ESTABLISHED
SMALL_WALL_NOT_CROSSED
```

This is a read-only compatibility audit of the evidence inherited from
`codex/family10h-tomography-repair` at
`cf6d49b0a46d25520a9ce61a06b1f90533058186`. It changes no retained evidence,
threshold, classification, source package, run archive, or authorization record.

The exact source paths and Git blob identities are frozen in
`I1_COMPATIBILITY_MATRIX.json` and verified by `i1_compatibility_audit.py`.

## 1. What survives as a usable calibration surface

Two post-source observables are supported strongly enough to carry into synthetic design.

### Scalar q coordinate

The sealed v1.1 evidence retains:

```text
D_single slope per q = 1.8612688337053558
linear regression R^2 = 0.9940328492833816
nearest-q accuracy = 1.0
```

This is a confirmed scalar calibration coordinate. The symbol `R2` used in the old
operator-dimension report means the statistical coefficient of determination, not the
CAT_CAS R2 restoration tier. It supplies no restoration evidence.

### Local paired differential

The retained positive claim ceiling is:

```text
PUBLIC_POST_SOURCE_LOCAL_PRIMARY_MINUS_SHAM_DIFFERENTIAL_CONFIRMED
```

with:

```text
D_local = R_primary - R_sham
```

The prospective local law requires positive `R_primary`, positive `D_local`, survival
across the declared one-factor strata, and generic controls bounded by `0.25 * D_local`.
This is a useful local post-source response coordinate. It is not a complete carrier
state, path relation, transport operator, independent accumulator, or restoration law.

## 2. What does not survive as a transport coordinate

### No stable second operator axis

The retrospective operator-dimension audit found residual rank beyond one, but no second
axis remained stable across the required session, replicate, mapping, delay, source-order,
and query-order strata. The deep hunt reached the same decision after testing order,
Hadamard, factorial, route-referenced, timing-normalized, and source-order-aligned
coordinates.

The minimum second-axis alignment across required strata was approximately:

```text
0.011908297788603826
```

Therefore residual dimensionality cannot be promoted into a physical carrier coordinate.

### Exploratory composition signal did not reproduce under the required controls

The first composition-loop screen produced:

```text
Omega_alive = 0.025081683677790033
source_off / alive magnitude = 0.06326050530086345
dead / alive magnitude = 0.2979268569177624
all one-factor signs matched
```

That made it a legitimate exploratory lead.

The reset follow-up produced:

```text
Omega_alive = 0.01999961551939171
source_off / alive magnitude = 0.822584339306217
reset / alive magnitude = 0.6670017174979355
all one-factor signs matched = false
```

Later balanced-phase, palindrome, phase-correction, replay, offset, orientation, and
source-map-odd screens also failed their declared controls. The strongest current
composition result is therefore a rejected exploratory schedule-order effect, not a
receiver-side connection law.

### Retrospective scalar replay failure is not H7

The ridge scalar-replay adversary failed to predict the retrospective order candidates on
held-out nuisance-factor levels. That is useful negative evidence against one specific
model. It does not pass H7 because:

- the candidate coordinates themselves did not survive the physical strata;
- the adversary class was not frozen prospectively for a new transport campaign;
- no source-preparation capacity bound was measured for the challenge family;
- no post-source held-out word grammar existed;
- no carrier or accumulator intervention was performed.

The allowed interpretation is:

```text
RETROSPECTIVE_SCALAR_REPLAY_MODEL_FAILED
```

not:

```text
BOUNDED_REPLAY_CLASS_REJECTED
ANSWER_CACHE_EXCLUDED
```

## 3. H0-H7 mapping

| Gate | I1 status | Retained basis | Blocking requirement |
|---|---|---|---|
| H0 | `PARTIAL_CALIBRATION_ONLY` | Confirmed `D_single` and `D_local` observables | No frozen multivariate carrier state `E`, complete disturbance law, or equivalence region |
| H1 | `NOT_ESTABLISHED` | Controlled coherence primitive and source-authored relation schedules exist | No receiver-side post-source maps `T_A,T_B:E->E` |
| H2 | `NOT_ESTABLISHED` | Reset and flush controls were exercised | No left and right inverse; reset controls failed and are destructive rather than inverse transport |
| H3 | `NOT_ESTABLISHED` | One exploratory composition contrast was nonzero | No frozen map composition predicting held-out words across required strata |
| H4 | `ORDER_EFFECT_OBSERVED_BUT_CONNECTION_LAW_NOT_ESTABLISHED` | Multiple order contrasts were measured | Replay, route/common-mode, source-off, reset, and stability controls failed |
| H5 | `NOT_TESTED` | Endpoint PMU readouts exist | No independent accumulator causally coupled to transport |
| H6 | `NOT_ESTABLISHED` | Byte and digest closure often passed | No state-wide inverse restoration with retained independent output; reset screens failed |
| H7 | `DIAGNOSTIC_ADVERSARY_ONLY` | One retrospective scalar replay model failed | No prospective bounded adversary, capacity bound, held-out word grammar, or causal intervention |

No gate is passed by I1.

## 4. Candidate primitives available to I2

I2 may catalog, but not yet freeze, the following physical-operation families:

```text
remote_store_same_value ownership-intent operation
relation-preserving layout
relation-swapped layout
relation-distance-shifted layout
relation-sham layout
R0 then R1 schedule
R1 then R0 schedule
source-off, dead-source, carrier-off, route-pressure, random, and neutral controls
```

These are ingredients, not generator maps. In particular, `R0` and `R1` are schedule
labels in retained source-authored experiments. I1 does not relabel them as receiver-side
operators `A` and `B`.

## 5. I2 admission law

I2 may proceed only as a synthetic and read-only generator-candidate catalog. For each
candidate operation it must record:

```text
physical action
acting process and core
whether it can occur after source death
candidate carrier coordinates it may transform
operation multiset and timing cost
left-inverse candidate
right-inverse candidate
known null and confound controls
retained evidence that supports or kills it
```

A candidate may enter synthetic transport tests only when the receiver can execute it
after source death without source IPC, hidden labels, answer-conditioned preparation, or
hardware access outside the retained access model.

I2 may not:

```text
fit a transport map to rejected exploratory coordinates
reuse a reset as an inverse
promote a schedule label into a physical operator by naming alone
inherit historical live authorization from the parent branch
contact the target
```

The retained autonomous goal state records historical lab contact authorization for the
old branch. That authority is not inherited by this successor package. The successor's
scope lock remains `NO_LIVE_EXECUTION_AUTHORIZED`.

## 6. Exact next target

The next artifact is:

```text
I2_RECEIVER_SIDE_GENERATOR_CANDIDATE_CATALOG
```

It must answer one concrete question:

> Which operations already present in the Family 10h runtime can be executed entirely by
> the receiver after verified source death and can plausibly act as repeatable maps on the
> retained calibration coordinates without merely replaying the public word?

Only after that catalog passes a no-smuggle and capability audit may synthetic two-sided
inverse and matched-commutator qualification begin.
