# Phase-QEMU V4 finite-MTC closed-probe findings

## Result

The single-global-closed-probe route is closed for a fixed finite UMTC under
the exact M262 descriptor class.

A whole-region boundary-parallel Wilson loop sees only the region's total
simple charge.  It is scalar on every internal fusion-multiplicity state, and
all such simple loops span only the fixed finite charge-projector algebra.
Growing fusion multiplicity can therefore coexist with observable rank one on
each definite-charge block.

The exact calibrations agree between production and a source-independent
reference:

| Category/query | One-loop amplitude | Return probability | Meaning |
|---|---:|---:|---|
| Semion `s,s` | `-1` | `1` | deterministic scalar sign |
| Ising `sigma,psi` | `-1` | `1` | deterministic finite-label sign |
| Ising `sigma,sigma` | `0` | `0` | original vacuum probe channel absent |
| Fibonacci `tau,tau` | `-phi^-2` | `phi^-4` | two nonzero coherent outcomes |

Two Ising `sigma,sigma` loops and five Fibonacci `tau,tau` loops realign the
finite-order channel phases.  This blocks any claim that a nonunit single-loop
amplitude permanently prevents exact return.

## Catalytic diagnostic

For total-`psi` Ising carriers of multiplicity dimensions
`1,2,4,8,16`, the bounded transaction executes a `sigma` probe (`-1`, bit 1),
restores the exact returned value, then consumes that value with a distinct
`psi` probe (`+1`, bit 0).  One preparation is used.  The result survives the
public inverse in each generation.

This is functional exact value restoration and reuse.  It does not establish
same-backing reuse, QEMU/CATVM custody, or a physical echo.

For Fibonacci `tau,tau`, the vacuum-return and complementary probabilities
are both nonzero.  A coherently copied which-outcome boundary has exact
Schmidt rank two.  Retaining it prevents factorized carrier/probe restoration;
the response remains unauthorized.  This obstruction is explicitly limited
to strictly intermediate probability.  The zero and unit endpoints have
different semantics.

## Resource conclusion

The strongest fixed-theory comparator wins immediately: validate the finite
category and query the exact `K x K` monodromy-character table.  A whole-region
closed loop does not read the growing internal fusion geometry.

This is not an all-anyon no-go.  The result says nothing about noncentral
constituent braids, open ribbons, tube-algebra coupons, multiple regions,
higher genus, adaptive measurement, or growing link networks.  Those
operations can access a larger algebra and may have nontrivial computational
complexity.

## Disposition

```text
SINGLE_GLOBAL_CLOSED_PROBE_HOLONOMY_IN_A_FIXED_FINITE_UMTC_IS_MULTIPLICITY_BLIND_AND_REDUCES_TO_A_CONSTANT_SIZE_TOTAL_CHARGE_MONODROMY_TABLE
```

Do not add more central-loop category fixtures.  The next materially different
target is:

```text
NONCENTRAL_INTERACTING_SCATTERING_OR_FLOQUET_EIGENPHASE_WITH_PREPARED_EIGENSTATE_COST_REFERENCE_COMPLETE_FACTORIZATION_AND_GROWING_RELATIONAL_INVARIANT
```

It must make the factorized boundary phase depend on growing relational data,
not a supplied eigenlabel, conserved charge, finite symmetry sector,
stabilizer/Gaussian invariant, or preparation receipt.  M257 remains
controlling for an equal-access deterministic software forward shadow.

## Claim ceiling

```text
ABSTRACT_EXACT_FIXED_FINITE_UMTC_SINGLE_SIMPLE_PROBE_GLOBAL_DISK_ENCIRCLEMENT_WITH_DECLARED_TOTAL_CHARGE_AND_SEMION_ISING_FIBONACCI_FIXTURES_ONLY
```

No physical anyon, coherent interferometer, physical restoration, QEMU device,
advantage, Small Wall crossing, unbounded compute, or bit-to-pi replacement is
established.
