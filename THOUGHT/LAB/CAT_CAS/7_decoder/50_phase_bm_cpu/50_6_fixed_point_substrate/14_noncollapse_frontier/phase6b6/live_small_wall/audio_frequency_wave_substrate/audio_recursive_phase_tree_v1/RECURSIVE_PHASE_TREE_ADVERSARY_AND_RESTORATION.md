# Recursive Phase Tree Adversary and Restoration Law

Status: `FROZEN_AND_EXECUTED_R0`

## Adversary Registry

| Arm | Construction | Required outcome |
| --- | --- | --- |
| Exact hierarchy | A queried by committed A | response at least `1-1e-12` |
| Wrong hierarchy | A queried by B | magnitude at most `0.98` |
| Subtree permutation | same nodes under B parent-child geometry | exact-minus-wrong gap at least `0.02` |
| Parent-child phase scramble | canonical cyclic local-phase reassignment | exact-minus-scramble gap at least `0.02` |
| Flat multitone replacement | all child carriers flattened into root phase | exact-minus-flat gap at least `0.02` |
| Amplitude-only | compare committed A/B magnitudes | null within `2e-7` |
| Spectrum magnitude | deterministic energy-balanced spectral phase mask | same spectrum within `1e-12`, different query |
| Global Z2 | committed A minus versus A plus | whole-beam negation; tree structure unchanged |
| Correct inverse | conjugate committed A beam | restore within `2e-7` |
| Wrong inverse | canonical local-phase-permuted A | error at least `0.05` |
| Reordered inverse | B parent-child geometry | error at least `0.05` |
| Metadata stripping | add LIST, parse, strip to minimal bytes | identical samples and original minimal bytes |
| Serialization mutation | unknown key, duplicate key, whitespace mutation | strict rejection |
| Fixture substitution | common phase-rotated WAV under A label | semantic binding rejection |

## Spectrum Boundary

No exact same-spectrum, different-hierarchy recursive tree is claimed. The spectrum arm
is explicitly a `spectrum_matched_non_tree` generated by applying a deterministic
energy-balanced `+1/-1` spectral phase mask. It demonstrates only that FFT magnitude is
not sufficient for the declared matched hierarchy query. It is never labeled a tree.

## Restoration Boundary

The borrowed object is a deterministic complex software tape. The forward operator is
pointwise multiplication by the parsed committed A beam. Correct restoration uses that
beam's conjugate. The wrong arm permutes local phase assignments; the reordered arm uses
the same node multiset under hierarchy B. Both must remain outside the frozen restoration
region.

This is R0-like digital algebra. There is no physical carrier, source disconnect,
measurement disturbance, physical baseline distribution, or physical R2 claim.

## Anti-Smuggle Law

Tree JSON and the manifest may contain public generator parameters and structural
identity, but no expected response, winning label, restoration answer, or hidden query.
Diagnostic scalars are computed after the complete beam exists and never feed an update.
