# Growing Rotor Dynamic Dihedral Phase-Frame Review

## Decision

`INDEPENDENTLY_VERIFIED_SOURCE_LOCAL` at
`SEPARATE_REFERENCE_PARITY`, with `EXACT_ALGEBRAIC_RESTORATION`.

A four-integer descriptor can carry the public Fourier basis label through the
288-gate forward endpoint and 289-gate inverse endpoint while the 2,277
momentum-bracelet coefficients remain resident. This is public basis-label
transport: it performs no arithmetic on those coefficients and retains no
Fourier matrix, occupation vector, dense quotient kernel, permanent work, or
transition plan in the accepted path.

The physical position-phase diagonal is not coefficientwise in that
transported basis. Its exact conjugate is the M204 reflection-paired
eight-channel scattering stream. Consequently the moving frame closes, but it
retains M204's 5,697,720 sorted searches, 5,534,928 fanout candidates, and the
identical compact classical recurrence. It does not remove the obstruction
reported by M204-M206.

## Separate reference parity

The separate oracle imports no CAT_CAS production module. For the complete
Rotor-2 sector over the same F103 order-17 root, it independently enumerates
153 occupations, nine momentum bracelets, and the nine-cell reflection-closed
zero-total position sector. It builds the quotient Fourier and inverse kernels
from direct two-particle permanent enumeration. Their product is exactly the
identity.

The oracle then constructs the transported position diagonal by explicit
matrix multiplication and independently constructs the scattering operator
from occupation moves. They agree on every one of the `9 x 9` cells. Treating
the position diagonal as coefficientwise in momentum-bracelet coordinates
instead disagrees on 79 cells. This makes the conjugation identity
transferable at Rotor-2, but it does not independently reexecute the Rotor-6
production path.

Rotor-6 parity is therefore source-local and relies on M204's separately
sealed, independently reexecuted boundary and signature-order commitment.
The qualifier compares M207's primary and reuse boundaries and primary
signature-order commitment against the M204 seal. It does not promote M207 to
an independent Rotor-6 oracle result.

## Restoration and controls

For the primary program, unrelated reuse program, and fresh reuse comparison,
the source, target, wave-port, and frame backing objects retain identity.
Restoration rematerializes the exact F103 target, subtracts it on the same
borrowed target backing, applies the exact public frame release, clears the
wave port, and advances the resident carrier generation. Restoration error is
zero and fresh/restored reuse agrees in both boundary and state commitments.
No snapshot or baseline reload participates.

Wrong frame owner, wrong public stage, premature live-frame projection, and a
missing frame inverse are detected. The missing word inverse leaves 2,257
nonzero target cells. The direct-process package does not claim CATVM custody
or atomic response ordering.

## Resource and claim ceiling

The accepted active state is 6,843 F103 cells/descriptor integers plus 8,943
retained public topology descriptor integers. The public-frame verification
uses 578 field cells and target reconstruction uses 38,709 histogram scalars;
both are verification-only and reported outside the accepted state. Python
objects, arbitrary-width integers, allocator/interpreter memory, native
library state, timing, and whole-process peaks remain excluded rather than
counted as zero.

The strongest matched baselines are the identical four-integer public frame
descriptor plus the M204 eight-channel vector stream, and M204 itself.

Exact ceiling:

```text
GRID17_EXCHANGE_SYMMETRIC_ROTATION_REFLECTION_INVARIANT_ROTOR6_F103_ROOT72_DEPTH1_PRIMARY_REUSE_DIRECT_PROCESS_PUBLIC_ENDPOINT_FRAME_DESCRIPTOR_AND_M204_CONJUGATE_ONLY
```

This result establishes no distinct phase resource, computational advantage,
Small Wall crossing, CATVM custody, physical waveform execution, replacement
of physical bits with pi, or unbounded catalytic computation. Because the
only exact closing conjugate is the already-measured M204 recurrence, this is
the final tested structural repair in the Rotor-6 route; the successor must
change the phase coupling law rather than add another rotor frame variant.
