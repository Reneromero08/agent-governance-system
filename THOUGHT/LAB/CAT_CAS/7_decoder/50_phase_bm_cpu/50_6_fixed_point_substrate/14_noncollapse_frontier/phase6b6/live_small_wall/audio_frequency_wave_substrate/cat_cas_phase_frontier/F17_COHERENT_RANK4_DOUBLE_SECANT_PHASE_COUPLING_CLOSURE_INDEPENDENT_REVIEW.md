# M131 Independent Review: Rank-Four Double-Secant Closure

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Resident four-component phase-carrier restoration:
`EXACT_ALGEBRAIC_RESTORATION`

Transient coupling, projection, commitment, catalecticant, compiler,
full-occupation-oracle, and classical-baseline buffers:
`NO_RESTORATION_CLAIM`

The reviewed scientific source is commit
`7fc59e806cc49298c44cd9d16ea2e309358d7de4`. M131 resolves the precise
M130 lower-bound gap without asserting a repeated-coupling law.

## Verified mechanism

Both couplings have the declared form `C_eta=I+eta*R`, where `R` swaps modes
zero and one and `R^2=I`. The two couplings therefore commute with each
other. The demonstrated noncommutation is the boundary-visible ordering of
the second coupling with the interleaved first consumer module. The exact
inverse remains `(I-eta*R)/(1-eta^2)`.

Starting from one coherent component, the first coupling produces two. After
module A, the second coupling produces four. A separately reconstructed
normalized degree-two catalecticant has a nonzero fixed `4x4` minor in every
declared case. Each coherent component contributes one rank-one row/column
outer product, so the nonzero minor gives rank at least four; the explicit
four-component representation gives rank at most four. The exact normalized
divided-power secant rank is therefore four.

The ordinary symmetric Waring-rank interpretation is made only for the four
characteristic-zero `Q(zeta17)` cases at `k=4,8,16,32`. The `F103/F137`
cases through `k=128` are structural normalized divided-power evidence, not
an unrestricted characteristic-positive Waring-rank claim.

The accepted carrier has four weights and four 17-coordinate coherent
vectors, or 72 field cells. The largest coupling inverse forms eight logical
component records, or 144 field cells, before exact duplicate cancellation.
Only the final `(k-1,1,0,...,0)` occupation scalar is projected. Reverse
module B, second coupling, module A, and first coupling execution restores
the seed and unloads it to exact zero on the same backing. Unrelated
`PRIMARY k=8` then `REUSE k=16` execution agrees with a fresh carrier in
boundary and reported resource signature, advances generation to two, and
uses neither snapshot reload nor retained inverse history.

## Independent reconstruction

The oracle does not import the M131 production module. It separately compiles
the public word, reconstructs both couplings, the four-component chart,
projection, reverse execution, exact duplicate merge, and folded endpoint
baseline. Its catalecticant determinant uses an independent 24-term Leibniz
expansion rather than production Gaussian elimination.

All 16 transaction commitments, boundaries, resource identities, and rank
certificates agree. Full `H(4)=4,845` occupation reexecution in `Q(zeta17)`,
`F103`, and `F137` agrees after coupling one, module A, coupling two, module
B, final projection, and the complete reverse path. Dense and chart states
restore exactly in all three algebras.

Omitting the second coupling makes the selected four-by-four minor vanish and
changes the boundary. Coupling/module reorder, missing inverse, wrong second
coupling inverse, reordered inverse, singular `eta=+/-1`, rank-two capacity,
premature projection, wrong ownership, null carrier, and missing exact merge
controls are detected. Generated-term count is explicitly not used without
the catalecticant lower bound. Intermediate components, amplitudes, and minor
values are not serialized.

## Matched baseline and ceiling

For descriptor-runtime execution, compact classical software uses the
identical four-component 72-cell recurrence. For each sealed word, the
stronger warm baseline folds every public weight into its mode-one endpoint
and retains eight total scalars after 72 compiler working cells. Its
commitment and boundary are independently reproduced. No computational
advantage is established.

The strict ceiling is the declared two-coupling, two-consumer public program,
exact `Q(zeta17)` at `k=4..32`, structural dual-field parity at `k=4..128`,
and direct-process software execution. Exact payload-height tuples remain
package-local, and Python containers, allocator/native-library storage,
bigint internals, hashlib state, bit-operation complexity, and whole-process
peaks remain excluded.

M131 does not establish a third or unbounded coupling-rank law, fixed-rank
unbounded-depth closure, general Gaussian closure, arbitrary secant input
closure, CATVM custody, a distinct phase resource, computational advantage,
Small Wall crossing, physical waveform execution, replacement of physical
bits with pi, or unbounded catalytic computation.

The next experiment should derive a symbolic iterated-coupling rank-growth law
or a fixed-rank Gaussian phase-chart no-go without enumerating component
assignments. It should not merely add one rank-eight fixture.
