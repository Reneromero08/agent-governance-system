# Necklace Coherence Small Wall Diagnostic

## Result

One bounded matched resource diagnostic now compares:

```text
coherent in-place Hermitian necklace generator
initial-and-each-step necklace-basis dephasing sham
best matched compact classical complex recurrence
```

The bounded claim is:

```text
BOUNDED_MATCHED_COHERENT_INITIAL_AND_EACH_STEP_NECKLACE_DEPHASED_CLASSICAL_GENERATOR_SMALL_WALL_RESOURCE_DIAGNOSTIC
```

Evidence:

```text
/tmp/four-rotor-necklace-coherence-triad-final.rAd8AR
```

The tested ceiling is direct-process, exchange-symmetric,
rotation-invariant, grid 17, four rotors, depth eight, complex128, and
software only.

## Matched arms

All arms use the same public grid, four-rotor carrier family, depth-eight
program, tag zero, 285-cell necklace interface, and seven-value collision-bin
boundary. No protocol is used, so logical protocol traffic is zero for every
arm.

The coherent phase arm and the best compact classical arm intentionally run
the same executable complex recurrence. This is the strongest matched
classical software baseline, not an expanded dense surrogate. Their primary
and reuse boundaries agree exactly.

Both coherent arms perform actual inverse restoration and actual restored
carrier reuse:

- primary restoration error: `1.3877500627453903e-14`
- generation-two restoration error: `1.7274307843759177e-14`
- matched primary boundary error: `0`
- matched reuse boundary error: `0`

The dephased arm performs exact complete dephasing in the
global-rotation-necklace basis before step zero and after every free step. If
`p_s = w_s |a_s|^2`, its streamed Markov kernel is:

```text
P(t | s) = (w_t / w_s) |U_rep(t,s)|^2
```

The probability sum remains within `2.220446049250313e-15` of one.
Dephasing is irreversible, so this arm reloads a disclosed 4,560-byte
snapshot and is not accepted as catalytic restoration.

## Coherence diagnosis

The maximum boundary difference between coherent and dephased execution is:

```text
0.023504205264602895
```

This establishes that initial-plus-interstep necklace coherence changes the
tested boundary. It does not isolate the contribution of the collision phase
from the initial coherence or free-step coherence.

The dephased path streams 649,800 transition coefficients and 265,118,400
permanent assignment terms. The result therefore does not claim that
dephasing is a cheaper matched baseline.

Missing, wrong, and applicably reordered inverse controls separate by
`1.4040897828772636`, `1.3464039441456184`, and
`1.075079787302776`.

## Resource and timing scope

Coherent and matched-classical lifecycle payloads:

- engine: 33,470 bytes
- verification baseline: 4,560 bytes
- projection boundary: 56 bytes
- lifecycle peak: 38,142 bytes

Dephased snapshot sham:

- two probability vectors: 4,560 bytes
- public topology: 10,532 bytes
- transition scratch: 254 bytes
- forward payload: 24,522 bytes
- full snapshot/reuse lifecycle peak: 42,702 bytes
- snapshot creation: 4,560 bytes
- snapshot reload: 4,560 bytes

The conservative all-arm harness peak is 66,894 explicit bytes.

Coherent/classical timing covers forward, projection, and actual inverse.
Dephased timing covers forward only and excludes snapshot recovery and reuse.
The timings are intentionally not compared and support no performance claim.

## Scientific conclusion

Coherence is causally relevant to the tested phase boundary, but software
phase arithmetic is not yet a resource unavailable to the best compact
classical method: that method is the identical complex recurrence.

The diagnostic does not establish a distinct phase resource, computational
advantage, Small Wall crossing, unbounded computation, or physical execution.
The obstruction is:

```text
COHERENCE_IS_CAUSAL_BUT_IDENTICAL_COMPACT_CLASSICAL_COMPLEX_RECURRENCE_INHERITS_IT
```

The next selected phase-owned experiment is:

```text
COHERENCE_DEPENDENT_OPEN_RELATIONAL_CATALYTIC_INFERENCE_ON_NECKLACE_CARRIER
```

It must expose typed public evidence ports, retain the prior/evidence
composition as unresolved necklace phase state, project only a final
inference boundary, restore the actual carrier, and reuse it. It must compare
against the same compact classical complex recurrence and must not treat
coherence-versus-dephasing separation as computational leverage.
