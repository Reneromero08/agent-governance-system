# Independent reexecution review: Rotor-6 coherent momentum-wave stream

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`

Restoration classification: `EXACT_ALGEBRAIC_RESTORATION`

Result:
`PASS_CHANNEL_FUSION_REDUCES_ORBIT_AND_DECODE_WORK_BUT_NOT_SEARCH_OR_CLASSICAL_EQUIVALENCE`

For the declared F103 Rotor-6 fixture, one owner-typed eight-cell momentum
wave port can hold all reflection-paired channel values for one necklace and
share the two source-orbit traversals across those channels. The exact final
state, boundary, restoration, and unrelated reuse match M203.

This is an exact software vector-superposition and traversal-sharing result.
It is not evidence of physical simultaneous superposition, a distinct phase
resource, or computational advantage. The strongest compact classical
baseline is the identical eight-coordinate vector stream.

## Production mechanism

The production mechanism preserves the exact factor identity

```text
K_q = A_-q A_q - rotor_count I
```

and M203's implicit-dihedral public topology. For each necklace it:

1. decodes the source histogram once;
2. leases one eight-cell port bound to the declared channel type, generation,
   and necklace owner;
3. computes the eight paired momentum values into that resident backing;
4. traverses the original source orbit once while consuming all channels;
5. constructs the reflection once and traverses its orbit once;
6. clears and releases all eight cells before the next necklace.

No individual channel is projected while the port is live. The complete
forward output is committed and released before inverse rematerialization.
Controls reject wrong type, wrong owner, premature port projection, null
carrier, missing inverse, wrong inverse, reordered noncommuting inverse, and a
wrong reflection law.

## Independent construction

The independent oracle imports only the prior independent implicit-dihedral
oracle. It imports no production wave, M203, M202, or M199 module. It separately
implements the eight-cell port, fused orbit schedule, transaction, controls,
and resource law.

The oracle compares the wave result against:

- its own compact scalar-stream recurrence;
- the prior separate 684,624-term direct two-body operator; and
- the prior separate reflection-paired factor construction.

All three comparisons have zero mismatched cells. Production and oracle agree
exactly on topology commitment, primary state commitment, primary boundary
83, restored and fresh reuse boundary 70, same-backing restoration, resource
counts, and negative claim boundaries. The oracle's full tuple topology and
494,496 plan entries are verification-only state, not accepted-path state.

## Resource law

The accepted numeric state is:

```text
2,277 source + 2,277 target + 2,277 output + 8 wave cells
= 6,839 field cells
```

Adding M203's 8,943 public topology integers gives 15,782 warm named slots,
seven more than the scalar stream. The fixed-width logical payload is 244,641
bits. The public source compiler still sets the full-lifecycle logical peak at
31,878 slots.

One forward scattering performs:

```text
4,389 wave-port leases and releases
35,112 wave-channel value computations and clear writes
149,226 shared source-orbit rotations
2,880,786 histogram digit decodes
2,842,077 reflection-cell operations
5,534,928 inverse fanout candidates
5,697,720 sorted-code searches
68,372,640 conservative comparison upper bound
331,704 accepted one-body contributions
```

Relative to M203, channel fusion removes 1,044,582 repeated source-orbit
rotations, 522,291 histogram digit decodes, and 522,291 reflection-cell
operations. It removes no fanout candidate and no sorted search. These are
logical operation counts, not a runtime-speed claim. The comparison count
remains a conservative upper bound, not an instrumented exact count.

Python containers, tuples, bisect, arbitrary-width integers, allocator,
interpreter, native-library memory, timing, and whole-process peaks are
excluded and therefore are not zero. Production's M203 scalar comparison and
the oracle's tuple topology, direct operator, factor plans, and scalar stream
are verification-only paths.

## Restoration and claim ceiling

Primary, reuse, and fresh-reuse transactions restore the actual source,
target, output, and eight-cell port backings exactly over F103. No snapshot or
baseline reload is used. Reuse advances restoration generation to two.

The result is limited to grid 17, six exchange-symmetric rotors, the declared
rotation- and reflection-invariant sector, F103 root 72, depth-one primary and
reuse programs, direct-process software, and this eight-channel
implicit-dihedral factor stream.

It does not establish CATVM custody, general open-port algebra, arbitrary-
rotor transfer, physical waveform execution, physical simultaneous
superposition, a distinct phase resource, computational advantage, a Small
Wall crossing, replacement of physical bits with pi, catalytic inference, or
unbounded catalytic computation. The remaining obstruction is the unchanged
fanout/search recurrence and its identical compact classical implementation.
