# F103 C102 public-continuation observability independent reexecution review

## Decision

`INDEPENDENTLY_VERIFIED_STRICT_SCOPE`

Verification level: `INDEPENDENT_ORACLE_REEXECUTION`  
Underlying carrier restoration class: `EXACT_ALGEBRAIC_RESTORATION`  
Expanded tangent diagnostic restoration class: `NO_RESTORATION_CLAIM`

## Reexecution boundary

M159 fixes interface C5, the two public M158 topology families, one publicly
derived source location, all 102 directions of its `F103[C102]` A-register
polynomial, consecutive continuation depths 1 through 24, and 25 explicit
public final-only B-coordinate projections per depth. The production tangent
certificate therefore has 600 rows and 102 columns for each family.

The scalar oracle imports neither M159 production, M158 production, nor NumPy.
It reuses only the separately qualified scalar M157 implementation of the
unchanged linear group-algebra operations, reconstructs the M158 shear formulas,
and obtains every Jacobian column from complete plus/minus forward trajectories.
Because A evolves linearly and B receives only linear transforms plus quadratic
functions of A, the final B boundary is quadratic in the chosen source chart;
centered differences over F103 are exact. An additional combined-direction
trajectory agrees with the linear combination of all basis columns.

Both complete 600-by-102 Jacobian byte commitments match production. Eight
declared production fields match independently: source node, every prefix rank,
final rank, and complete Jacobian commitment for both families.

## Preserved strict result

The cumulative public-continuation Jacobian reaches rank 102 in both declared
families: alternate first reaches 102 at depth 23 and primary first reaches 102
at depth 24. Thus, at these exact source charts, the declared continuation map
cannot factor through a regular algebraic local state of dimension below 102.
This closes the candidate smooth nonlinear local quotient left open by M158.

The conclusion is local and algebraic. It does not reject discontinuous,
singular, finite-set-specific, or global encodings, and it does not prove that
all source nodes, coordinates, interfaces, or topology families have the same
rank. Sparse depth sampling is insufficient: the depth-5 prefixes have ranks
20 and 25, so stopping there would have reported a spurious nullspace.

## Carrier and resource boundary

The accepted M158 carrier remains 45,900 F103 cells at C5, identical to the
strongest matched classical coefficient recurrence. The production tangent is
a verification-only expansion of 4,681,800 field cells; the 600-by-102 oracle
matrix uses 61,200 field cells. Neither is accepted as compact carrier state.
Allocator, native-library, and whole-process peaks remain excluded.

The underlying M158 transaction still demonstrates exact algebraic inverse
restoration, same-backing reuse, restoration generation two, and an unrelated
second boundary matching fresh execution without snapshot reload. M159 does not
claim restoration of its temporary tangent or oracle state.

## Claim ceiling

This package establishes no CATVM custody, shared same-source multi-consumer
port, distinct phase resource, computational advantage, Small Wall crossing,
physical waveform or silicon execution, replacement of physical bits with pi,
or unbounded computation. The next mechanism must change the phase state law or
test a precisely defined singular/global quotient; extending this same
observability fixture is not itself phase-native progress.
