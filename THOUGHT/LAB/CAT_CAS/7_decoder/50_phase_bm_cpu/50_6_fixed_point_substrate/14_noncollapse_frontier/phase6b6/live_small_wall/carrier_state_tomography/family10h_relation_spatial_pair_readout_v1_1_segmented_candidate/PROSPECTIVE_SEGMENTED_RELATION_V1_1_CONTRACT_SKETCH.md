# Prospective Segmented Relation-Spatial V1.1 Contract Sketch

Do not execute as a live authority document until converted into the full package and validated.

Predeclare a 10,240-row schedule: 2,048 primary relation rows first, followed by four 2,048-row R-shaped control segments. Use the same Family 10h source/receiver CPU boundary, carrier, work constants, relation formulas, and pair-sampling grammar as v1.0 unless a future source freeze explicitly changes them.

Adjudication must compute `R_spatial` separately by query segment. The positive result requires the primary segment to exceed its matched-permutation q99 and remain factor-stable, while all control segments remain within their own matched-permutation q99 and the primary/control separation is at least 4x.

No target-derived threshold revision is allowed after acquisition. No positive claim may exceed public spatial relation-coordinate readout. Small Wall crossing remains false.
