# Fixed-Schema QANF Small-Wall Obstruction Triad

## Status

This mutable checkpoint establishes:

```text
FIXED_SCHEMA_QANF_COMPACT_BASELINE_OBSTRUCTION_CONFIRMED_UNDER_MATCHED_CATVM_BOUNDARY
```

within:

```text
BOUNDED_LINUX_WARM_FIXED_SCHEMA_QANF_BASELINE_SNAPSHOT_IN_PLACE_RESOURCE_COMPARISON_REFERENCE_ONLY
```

This is a negative but useful Small-Wall result. It identifies why the
fixed-schema nonlinear CATVM proof cannot supply C5 leverage. It is not a
Small Wall crossing and does not weaken the established nonlinear carrier
law.

## Scientific question

The preceding CATVM proof establishes hidden nonlinear intermediate custody,
actual inverse restoration, and reuse for:

```text
F(a,b;u) = u + alpha + beta*a*b
G(u,c;v) = v + gamma + delta*u*c
J(v,e;d) = d + eta + theta*v*e
```

The triad asks whether that machine law has a resource distinction against
the best matched compact conventional computation for the same fixed public
schema.

It does not use batch count as problem size. The schema remains fixed at six
free public coefficient bits and a five-coefficient output in every run.

## Matched service boundary

One source builds three separate persistent services at compile time:

```text
B_compact   carrier-free compact conventional evaluator
S_snapshot  reviewed phase forward path plus snapshot reload
A_in_place  reviewed phase forward path plus actual inverse
```

All arms:

```text
parse the same four public programs
seal exact active and canonical program tables
use the same locked non-dumpable one-client Unix service boundary
unlink the socket after accept
install the same seccomp default-kill surface
receive the same public palindrome schedule
return byte-identical final-boundary packets
deny H/Z and every internal projection
emit zero service stdout and stderr
```

The controller contains the public variant schedule but no QANF evaluator or
expected boundary. Timed execution traffic is exactly nine request bytes and
22 response bytes per transaction in every arm.

The phase builds contain no compact-evaluator fallback symbol. The compact
build contains no phase symbol or carrier machinery.

## Three exact work laws

The compact evaluator recomputes:

```text
[1, eta, theta*gamma, theta*delta*alpha, theta*delta*beta]
```

on every request using the shared `theta*delta` term:

```text
W_B(B) = 4B ordinary Boolean ANDs
```

It has no result cache, lookup table, answer vector, carrier, snapshot, or
phase engine.

The snapshot sham runs the real native forward path and final decode:

```text
W_S(B) = 9B phase products
         + 51B carrier reads
         + 23B phase updates
         + 5B final decodes
         + B snapshot reloads of 368 working bytes
```

It performs no inverse and restoration generation remains zero.

The augmented arm runs the accepted CATVM lifecycle:

```text
W_A(B) = 18B phase products
         + 97B carrier reads
         + 46B phase updates
         + 5B final decodes
```

It performs no snapshot load. Every timed transaction executes an actual
inverse, and the warm and timed workloads consume one repeatedly restored
carrier.

## Warm rotated measurements

Each persistent service warms for 1,024 transactions, resets only public
work counters, and then runs one timed batch on CPU 0 at nice level 5. The
fixed arm order at each batch is:

```text
baseline, snapshot, in_place, in_place, snapshot, baseline
```

The public transaction schedule repeats:

```text
[primary, affine_sham, degree2, reuse,
 reuse, degree2, affine_sham, primary]
```

All 18 runs match all four independent-reference boundaries and have equal
timed packet and byte counts.

Mean wall observations were:

```text
batch     compact ns     snapshot ns     in-place ns     compact/in-place
1,024     10,277,323      13,390,417      12,963,641          0.793
4,096     37,551,061      47,359,900      53,619,430          0.700
16,384   156,882,340     200,183,066     212,788,896          0.737
```

These are bounded throughput observations, not mission Gamma. They establish
no performance advantage for the in-place CATVM arm.

## Capacity obstruction

The obstruction is stronger than the timing:

```text
free public coefficient bits                6
possible fixed-schema public programs      64
final coefficients per program              5
raw answer-vector upper bound              320 bits
compact exact evaluator                      constant size
```

The 320-bit upper bound is analytical only; no answer vector is materialized.
Because the exact conventional evaluator is constant-size and uses four
ANDs, repeating a fixed program cannot create a capacity separation, a
growing resource ratio, or C5.

## Resource boundary

Compiler and size probes account for active and sealed public programs,
mapped contexts, carrier and sealed carrier state, transaction comparison
snapshots, snapshot reload bytes, request and response buffers, binaries,
traffic, program-seal CPU time, service CPU time, and compiler-measured stack
frames.

```text
                              compact   snapshot   in-place
context structure               320      1,792      1,792 bytes
mapped context                 4,096      4,096      4,096 bytes
live carrier                       0        736        736 bytes
sealed carrier                     0        736        736 bytes
execution comparison snapshot      0        736        736 bytes
working reload / transaction       0        368          0 bytes
service binary                26,104     35,024     35,024 bytes
```

No total-memory advantage is claimed. The mapped context is one page in all
three builds, while the phase arms still carry additional functional state
and native work.

## Controls

The evidence includes:

```text
all four fixture boundaries versus independent reference
affine and degree-two non-affine applicability controls
same protocol and byte-identical timed execution traffic
attempted H and Z projection denial in all arms
embedded-NUL, oversized, and unknown request denial
null-carrier/startup rejection
analyzer and ASan/UBSan passes for every arm
nonempty post-custody traces for every arm
zero forbidden post-custody output channels
complete nonlinear CATVM predecessor regression
82 top-level provenance bindings plus nested CATVM/QANF bindings
```

## Adjudication and next frontier

The fixed schema is now a closed calibration and obstruction result:

```text
nonlinear CATVM machine law            established
fixed-schema compact baseline          strictly available
fixed-schema capacity separation       unavailable
C5                                     not established
SMALL_WALL_CROSSED                     false
```

Further batch repetition or longer QAND chains would not remove this
obstruction. The next mechanism must grow a real relation interface or graph
separator while keeping non-affine unresolved state compact. The selected
route is a width-parametric, many-to-many non-affine factor/TT relation
algebra with actual resident composition, exact inverse/reuse, and comparison
against its best compact conventional contraction—not another fixed-schema
fixture.

## Reproduction

```bash
evidence_dir=$(mktemp -d /tmp/qanf-small-wall.XXXXXX)
bash qualify_small_wall_qanf_obstruction.sh "$evidence_dir"
```

Reviewed evidence:

```text
/tmp/qanf-small-wall-fourth
```
