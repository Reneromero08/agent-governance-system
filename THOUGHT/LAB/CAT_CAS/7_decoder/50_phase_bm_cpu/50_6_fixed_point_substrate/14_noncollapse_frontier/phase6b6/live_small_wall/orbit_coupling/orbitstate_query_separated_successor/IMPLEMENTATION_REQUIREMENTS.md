# Implementation Requirements

Status: `FROZEN_DESIGN_CONSTRAINT`

No live executor is implemented or authorized by this design package.

Independent review status:

```text
QUERY_SEPARATED_ARCHITECTURE_BLOCKED
```

## Future Repair Package

Before any implementation package is frozen, a design repair package must define:

```text
R
P(R)
Q
h(P(R), q)
label action
relation mutation
joint-interaction observable J_q
query entropy and carrier-capacity law
all-query answer-cache adversary
exact physical preparation/handoff/query/restoration maps
receiver OS capability manifest
machine-readable expected tuple multiset
global executed event sequence
R2 forward-displacement plus equivalence contract
single authoritative mandatory-gate registry
```

## Future Package Identity

A future implementation must freeze:

```text
new science/run identity
new public randomization seed
same OrbitState source formulas only where they do not precompute query answer
same source/receiver boundary doctrine
same feature-freeze boundary
control-only carrier-gain or calibration law when needed
one consistent near-zero absolute law
strong-signal mapping and reversal law
both fresh replicates or stronger schedule pass
no target-derived gain fitting
no post-run threshold revision
```

## Metric Domains

Separate mathematical domains are mandatory:

```python
positive_gain_relative_error
complex_vector_relative_error
angular_error
norm_ratio_error
absolute_count_null
physical_restoration_distance
```

Every metric must specify:

```text
units
domain
denominator
zero behavior
threshold source
which observable it gates
```

Do not reuse count-domain floors for dimensionless quantities.

## Implementation Gates

Before any live authority exists, the implementation must pass:

```text
retired predecessor cannot execute
source cannot read receiver query
receiver cannot read private source data
exact schedule coverage enforced before decoding
scalar replay adversary rejected
query-separation violation classified custody-invalid
label invariance control passes
relation mutation control passes
geometry null kills
carrier-off kills
fixed-work semantic control passes
R2 restoration control suite passes
authoritative classification occurs only after copy-back/custody closure
```

## Evidence Bundle

The future evidence bundle must separate:

```text
raw
derived features
receipts
adjudication
controller custody
post-run audit
```

Derived files must be regenerable from raw data and sealed receipts. No summary-only
evidence is sufficient.

## Live Authority

This design package authorizes:

```text
0 target contacts
0 SSH/SCP/ping operations
0 PMU acquisitions
0 remote root creation
0 live executor runs
```

Any future live transaction requires a separately frozen package, exact head,
manifest hash, authority variables, review, and explicit user authorization.
