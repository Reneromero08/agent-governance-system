# Phase 6B Course Correction Addendum — Carrier Chart Before Reacquisition

**Status:** `ACTIVE_BINDING_ADDENDUM`  
**Date:** 2026-06-19  
**Base directive:** `COURSE_CORRECTION.md`  
**Scope:** Phase 6B.5 carrier validation and the transition into Phase 6B.6  
**Doctrine changed:** no  
**Execution order changed:** yes

---

## 1. Authority and supersession

This addendum preserves every ontological, no-smuggle, evidence, restoration, and boundary rule in `COURSE_CORRECTION.md`.

It supersedes only the stale operational instruction in the original Phase 6B.5 section that directly schedules new `trials=300/mode` acquisitions before adjudicating the receiver coordinate chart.

The following old sequence is no longer the immediate execution order:

```text
T48/T300 percentage-gate comparison
→ repeat or increase trials
→ close the old scalar conjunction
```

The binding sequence is now:

```text
raw carrier witness
→ theory-to-gate audit
→ transfer-aware analysis of the existing raw field
→ freeze the exact carrier claim and unresolved mechanism
→ choose the next physical control
→ external L4B.5B0 human review
→ authorized observability/operator acquisition
```

No new physical acquisition is authorized by this addendum.

---

## 2. Evidence that forced the correction

The T48 campaign established:

```text
14/14 structurally valid runs
37,757,575 retained raw records
24,864 reconstructable windows
silent carrier-off control valid
scramble unshared-schedule control valid
raw-to-summary regeneration complete
```

The frozen old conjunction remained `PARTIAL`.

The subsequent theory-to-gate audit established that the old conjunction combines distinct scientific objects:

```text
local metadata-tape restoration
mode transport
phase transport
execution-over-declaration
shared-schedule specificity
ideal sender-codebook concentration
metadata-leakage sanity
```

It also found:

1. the frozen contract reports seven gates while the historical analyzer reports nine;
2. the direct `rho` comparison assumes the receiver remains in the ideal sender basis despite an unidentified route/session transfer chart;
3. the `>=0.95` per-mode floor becomes a zero-error gate for the observed T48 bucket sizes;
4. ordered tone windows were collapsed into a static vector before path/order effects were identified.

Therefore another blind T48 repeat or T300 increase would test the same unresolved chart assumption more expensively. It would not yet target the unique missing mechanism.

---

## 3. Correct current state

```text
raw provenance: COMPLETE
raw reconstruction: COMPLETE
phase transport: STRONGLY SUPPORTED
executed-mode transport: STRONGLY SUPPORTED except route 4:5 seed 4
ideal sender-basis fidelity: NOT REPRODUCIBLE at T48
physical route/session transfer chart: NOT IDENTIFIED
strict carrier closure: PARTIAL
observability acquisition: NOT AUTHORIZED
```

The official `PARTIAL` result is preserved. No historical artifact is rewritten.

---

## 4. Immediate gate — Phase 6B.5C

The immediate gate is:

```text
Phase 6B.5C — Transfer-Aware Carrier Geometry Analysis
```

Use only the retained immutable T48 evidence.

The binding contract is:

```text
carrier_witness_closure/theory_gate_audit/NEXT_ANALYSIS_CONTRACT.md
```

Required questions:

1. Can a route/session receiver chart be estimated from preamble and even-real calibration rows only?
2. Do odd real and wrong rows preserve executed-mode geometry under that held-out chart?
3. Does declared phase act equivariantly after charting?
4. Do pseudo rows match the exact executed permutation better than canonical or unrelated models?
5. Are route-specific charts related by a stable conjugacy?
6. What ordered-path information is erased by static vector assembly?
7. Is route `4:5`, seed `4` a carrier failure, chart failure, transfer-regime shift, mixed failure, or unresolved?

The analysis must preserve complex relations and ordered path. It must not replace the object with a scalar candidate winner.

---

## 5. Decision after Phase 6B.5C

Only the completed transfer-aware analysis may select the next physical action.

Allowed next actions are:

```text
independent same-protocol session control
reversed/randomized tone-order control
explicit route-transfer calibration
versioned higher-N campaign
route-scope narrowing
carrier-claim narrowing
```

The next run must target the unresolved mechanism identified by Phase 6B.5C.

Forbidden move:

```text
increase trials until the old conjunction passes
```

---

## 6. Transition into Phase 6B.6

Phase 6B.6 remains blocked until:

```text
carrier scope is frozen or explicitly narrowed
L4B.5B0 external human review is accepted
explicit acquisition authorization is recorded
```

The transfer-aware chart analysis is still Phase 6B.5 carrier adjudication. It does not by itself establish a full observable physical state or predictive physical operator.

---

## 7. Unchanged doctrine and claim ceiling

The base course correction remains binding:

```text
THE ALGORITHM IS DEAD
unresolved relational geometry is primary
geometry bears memory
catalysis is borrow → transform → invariant → restore/close
measurement occurs at an explicit CollapseBoundary
carrier ≠ observable state ≠ operator ≠ restoration ≠ coupling ≠ crossing
```

Still not established:

```text
physical HoloGeometry
physical relational memory
identified complete physical operator
physical restoration
target-to-carrier coupling
orientation recovery
fold-odd invariant
Small Wall crossing
```
