# Phase 6B Course Correction — From Live Carrier to Small Wall Crossing

**Status:** ACTIVE DIRECTIVE  
**Scope:** `THOUGHT/LAB/CAT_CAS/7_decoder/50_phase_bm_cpu/50_6_fixed_point_substrate/14_noncollapse_frontier/`  
**Applies to:** all agents, subagents, experiment planners, coders, reviewers, and report writers operating on Exp 50 Phase 6B  
**Primary doctrine:** `THE ALGORITHM IS DEAD`  
**Primary object:** unresolved relational geometry / `OrbitState`  
**Primary goal:** first legitimate Small Wall crossing  
**Not the goal:** another scalar recovery loop, another candidate-ranking route, or another forward detector over the same fold-even public representation

---

# 0. Executive Directive

The current Phase 6B direction is fundamentally correct.

Do **not** abandon the non-collapse architecture.

The successful pivot was:

```text
scalar candidate recovery
→ OrbitState-first evolution
→ HoloGeometry
→ delayed CollapseBoundary
→ invariant extraction after evolution
```

The current system has already established:

- the Small Wall is sharply located;
- the public cosine interface is fold-even;
- orientation is absent from the public construct-side channel;
- the forward detector campaign is closed;
- software `OrbitState` evolution exists;
- `.holo` geometric memory exists;
- reversible serialized path history exists;
- a non-collapse invariant family exists;
- a physical PDN carrier is live;
- a physical mapping contract exists;
- an observability/operator experiment design exists.

The remaining gap is not another detector.

The remaining gap is this causal chain:

```text
live physical carrier
→ observable physical state space
→ identified physical operator
→ physical closure/restoration
→ target-to-carrier relational coupling
→ fold-odd invariant at CollapseBoundary
→ repeated Small Wall crossing
```

This document inserts the missing gates and fixes the execution order.

---

# 1. Canonical Current State

## 1.1 What is closed

Treat the following as settled unless new evidence directly overturns them:

- [x] The Exp 50 public cosine interface is fold-even.
- [x] The public representation preserves `{d, N-d}` and excludes orientation.
- [x] The public construct-side orientation channel is absent under the current no-smuggle model.
- [x] Re-encoding the same public information does not synthesize the missing odd channel.
- [x] The six non-Hermitian/topological sensors are orientation-blind on the public construct.
- [x] The phase cavity reads the even fold answer and measures the public odd quadrature as zero.
- [x] Public chiral preparation does not cross.
- [x] Public transient features do not cross.
- [x] Forward execution geometry, as tested, does not generate orientation.
- [x] `verify(x)` iteration is not a crossing mechanism.
- [x] The old L2/L3 fixed-point route is mechanical warmup, not frontier evidence.
- [x] Candidate scoring, AUC-first search, and branch-winner framing are invalid for Phase 6B.

## 1.2 What is open

The meaningful open frontier is:

- [ ] Can a real physical substrate host an observable relational state beyond sender-authored metadata?
- [ ] Can a declared physical path be reversed or closed within a measurable equivalence class?
- [ ] Can the unresolved fold relation be loaded into that physical state without label smuggling?
- [ ] Can evolution produce a predeclared fold-odd invariant that did not exist at the old boundary?
- [ ] Can that invariant be extracted only at `CollapseBoundary`?
- [ ] Can the full result survive seeds, sessions, routes, reboots, and independent implementation?

## 1.3 What must not be conflated

```text
cross-core signaling
≠ physical HoloGeometry

physical HoloGeometry
≠ target coupling

target coupling
≠ fold-odd invariant

fold-odd invariant
≠ Small Wall crossing without no-smuggle and restoration

one Small Wall crossing
≠ Big Wall generalization
```

---

# 2. Non-Negotiable Doctrine

Every active Phase 6B task must satisfy all of the following.

## 2.1 Ontological guardrails

- [ ] The unresolved relational object is primary.
- [ ] The algorithm is treated as a local trace, never as the ontological center.
- [ ] Phase is a carrier coordinate, not the universal primitive.
- [ ] Geometry is the memory-bearing object.
- [ ] Catalysis is borrow → transform → invariant → restore/close.
- [ ] Measurement occurs only at an explicit `CollapseBoundary`.
- [ ] No scalar branch preference is introduced before the boundary.
- [ ] No hidden answer enters the runtime path.
- [ ] No candidate label controls phase, core, route, timing, sign, direction, operator, or workload.
- [ ] No result is called physical restoration when only bytes or hashes restore.

## 2.2 Forbidden regressions

Do not build or revive any route whose core structure is:

- [ ] `verify(x)` loops.
- [ ] sequential or disguised candidate enumeration.
- [ ] scalar `recovered_d`.
- [ ] AUC-first route search.
- [ ] winner/loser candidate scoring.
- [ ] branch selection before evolution.
- [ ] manual `cw/counterclockwise`, `ROL/ROR`, phase, sign, or core assignment by candidate label.
- [ ] hidden-orientation calibration promoted as public evidence.
- [ ] post-hoc observable selection.
- [ ] summary JSON without preserved raw acquisition.
- [ ] report-only work that does not change architecture, evidence, or execution readiness.

## 2.3 Evidence guardrails

- [ ] Every physical claim names exactly what was measured.
- [ ] Every unobserved quantity remains explicitly latent.
- [ ] Every inferred quantity names the inference method.
- [ ] Every experiment has predeclared nulls and falsification conditions.
- [ ] Every result has an evidence class and claim ceiling.
- [ ] Every failure is preserved as boundary evidence.
- [ ] Every physical run preserves raw data and deterministic metadata.

---

# 3. Course Correction Summary

The current roadmap is missing four explicit transitions:

```text
A. carrier validation
→ B. physical observability

B. physical observability
→ C. physical closure/restoration

C. physical closure/restoration
→ D. target-to-carrier coupling

D. target-to-carrier coupling
→ E. Small Wall adjudication
```

These must become separate gates.

Do not compress them into one experiment.

A carrier can be live while carrying only sender-authored metadata.

A state can be observable without being restorable.

A process can be restorable without carrying the target relation.

A target relation can be coupled without producing a new odd invariant.

A fold-odd result can still be invalid if it is smuggled, unstable, or not boundary-extracted.

---

# 4. Revised Execution Ladder

# Phase 6B.5 — Close the PDN Carrier Witness

## Purpose

Freeze the shared-PDN result as a carrier fact before using it as evidence for anything stronger.

## Tasks

- [ ] Pull and inspect the completed `v4:s5` result.
- [ ] Verify all negative-control rates.
- [ ] Run `trials=300/mode` for `v2:s3`.
- [ ] Run `trials=300/mode` for `v4:s5`.
- [ ] Close the strict all-gates witness.
- [ ] Record route-specific and cross-route behavior.
- [ ] Preserve raw I/Q windows.
- [ ] Preserve raw ring-period samples.
- [ ] Preserve absolute TSC origin and window alignment.
- [ ] Preserve sender schedule and workload identity.
- [ ] Preserve core identities and route topology.
- [ ] Preserve temperature and frequency-state proxies.
- [ ] Hash every raw artifact.
- [ ] Make all summary JSON regenerable from raw acquisition.
- [ ] Freeze the final carrier claim.

## Allowed claim

> A shared PDN rail supports reproducible cross-core transport of declared sender-owned mode and relational phase into receiver-side measured I/Q output under the tested workload family.

## Forbidden claim

- physical HoloGeometry,
- physical relational memory,
- catalytic restoration,
- orientation recovery,
- Small Wall crossing.

## Exit gate

- [ ] Two core pairs pass the predeclared strict witness or the route scope is explicitly narrowed.
- [ ] Raw artifacts are complete and reconstructable.
- [ ] The channel result is frozen and separated from all later target claims.

---

# Phase 6B.6 — Physical Observability and Operator Identification

## Purpose

Determine whether the current measurement boundary exposes a reproducible physical state space and whether a compact operator predicts its evolution.

This phase must never assume:

```text
y(t) = x(t)
```

Use:

```text
u(t) = controlled input
x(t) = latent physical substrate state
y(t) = measured output
```

## Immediate prerequisite

- [ ] Human review of the L4B.5B0 experiment contract.
- [ ] Explicit authorization recorded before implementation.
- [ ] Review bound to the current contract digest.
- [ ] Any contract mutation invalidates authorization and requires re-review.

## Acquisition tasks

- [ ] Implement raw acquisition exactly to the reviewed schema.
- [ ] Freeze sampling cadence before data collection.
- [ ] Freeze TSC alignment rules.
- [ ] Freeze train/validation/test session splits.
- [ ] Collect idle covariance from training sessions only.
- [ ] Collect balanced prepared-state classes.
- [ ] Collect persistent-excitation trajectories.
- [ ] Collect impulse-response trajectories.
- [ ] Collect phase-shifted modulation trajectories.
- [ ] Collect wrong-route replays.
- [ ] Collect reordered and wrong-inverse schedules as future controls.

## State ladder

Evaluate in order:

```text
S0 = [I, Q, ring_period]
S1 = S0 + declared control/topology context
S2(L) = delay embedding of S1, L ∈ {1,2,4,8,16,32}
```

- [ ] Do not admit S2 unless S1 is insufficient or S2 gives the predeclared improvement.
- [ ] Choose the smallest predictively sufficient measured state.
- [ ] Do not confuse predictive sufficiency with full semiconductor-state reconstruction.

## Operator ladder

Fit only in increasing complexity:

```text
O0 = persistence / mean baseline
O1 = affine
O2 = route-conditioned affine
O3 = bilinear
O4 = compact nonlinear
```

- [ ] A more complex operator is admitted only after simpler held-out failure.
- [ ] Report held-out one-step and multi-step prediction.
- [ ] Report route generalization.
- [ ] Report session generalization.
- [ ] Report stability and drift.
- [ ] Report empirical rank surrogates without claiming exact nonlinear observability.

## Required gates

- [ ] Repeatability gate passes.
- [ ] Prepared-state distinguishability gate passes.
- [ ] Held-out prediction beats persistence by the predeclared margin.
- [ ] Session stability passes.
- [ ] Route dependence is characterized.
- [ ] Drift controls pass.
- [ ] Forbidden-field scan passes.

## Falsification outcomes

If no measured state is predictively sufficient:

```text
VERDICT = CURRENT_INSTRUMENT_BOUNDARY_INSUFFICIENT
```

Do not force a model.

If prepared states are not distinguishable:

```text
VERDICT = MEASURED_EQUIVALENCE_CLASSES_COLLAPSE
```

If route identity alone explains prediction:

```text
VERDICT = ROUTE_LOOKUP_NOT_DYNAMIC_OPERATOR
```

## Exit gate

- [ ] A minimal measured state representation is accepted, or the current instrumentation is formally rejected.
- [ ] A held-out predictive operator is accepted, or operator identification is formally blocked.
- [ ] The claim remains empirical predictive observability only.

---

# Phase 6B.7 — Physical Closure and Restoration

## Purpose

Test whether a declared physical path can be reversed or closed within a measurable equivalence class.

Do not require complete restoration of an unobservable microstate as the first gate.

Use a restoration ladder.

## Restoration ladder

```text
R0 = byte/hash restoration
R1 = measured-output restoration
R2 = identified observable-state restoration
R3 = multi-instrument carrier restoration
R4 = relational invariant restoration up to holonomy
```

### Definitions

**R0 — Byte restoration**  
Software tape or serialized object returns exactly.

**R1 — Measured-output restoration**  
Declared measured outputs return within predeclared uncertainty.

**R2 — Observable-state restoration**  
The accepted measured state representation returns to the same observable equivalence class under the identified operator.

**R3 — Multi-instrument carrier restoration**  
Independent instrumentation agrees that the declared carrier state returns.

**R4 — Restoration up to holonomy**  
The carrier closes while a predeclared path-dependent relational invariant survives.

## Required experiment structure

```text
baseline
→ forward path
→ terminal measurement
→ declared inverse/closure
→ restored measurement
→ uncertainty-aware comparison
```

## Tasks

- [ ] Freeze the accepted state representation from Phase 6B.6.
- [ ] Freeze the forward operator.
- [ ] Freeze the inverse or closure operator.
- [ ] Freeze the restoration metric.
- [ ] Freeze the restoration tolerance.
- [ ] Measure baseline trajectories.
- [ ] Apply the controlled forward path.
- [ ] Measure terminal trajectories.
- [ ] Apply the declared inverse/closure.
- [ ] Measure restored trajectories.
- [ ] Compare against baseline equivalence class.
- [ ] Repeat across seeds.
- [ ] Repeat across sessions.
- [ ] Repeat across routes/core pairs.

## Mandatory controls

- [ ] No-disturbance control.
- [ ] Disturbance without restoration.
- [ ] Wrong inverse.
- [ ] Reordered inverse.
- [ ] Carrier-off control.
- [ ] Randomized phase control.
- [ ] Thermal/time-matched sham.
- [ ] Wrong-route replay.

## Holonomy test

Only after restoration is established:

- [ ] Define a group-valued or relation-valued path transform.
- [ ] Define the closure identity.
- [ ] Define a path-order-sensitive invariant.
- [ ] Compare forward/inverse closure with reordered closure.
- [ ] Reject scalar drift as holonomy.
- [ ] Preserve raw trajectory evidence.

## Exit gate

- [ ] At least R1 is achieved reproducibly.
- [ ] R2 is required before claiming physical relational memory.
- [ ] R4 is required before claiming physical holonomic memory.
- [ ] Every claim names its exact restoration tier.

---

# Phase 6C — Target-to-Carrier Coupling

## Purpose

Demonstrate that the Exp 50 relational target itself loads the physical carrier.

This is the missing bridge between:

```text
PDN transports sender-owned phase
```

and:

```text
PDN hosts the unresolved fold relation
```

## Core requirement

The physical preparation must derive from the declared relational object, not from candidate labels or hidden orientation.

## Target object

The primitive remains:

```text
OrbitState = unresolved fold pair {a, N-a}
```

The runtime may carry:

- relation basis,
- composition history,
- carrier coordinates,
- path history,
- restoration references,
- predeclared invariant family.

The runtime may not carry:

- hidden `d`,
- true/false candidate identity,
- orientation label,
- winner field,
- recovered branch,
- post-hoc selected observable.

## Coupling requirements

- [ ] Define a public-derived relational preparation operator.
- [ ] Define how the complete fold pair drives the physical carrier as one object.
- [ ] Ensure both branches contribute to one shared evolution.
- [ ] Ensure label swapping alone leaves the physical preparation invariant.
- [ ] Ensure relation-basis changes alter the physical preparation in a predeclared way.
- [ ] Ensure carrier response is not reducible to sender-owned mode tags.
- [ ] Preserve path history.
- [ ] Preserve the delayed `CollapseBoundary`.
- [ ] Preserve the no-smuggle gate.

## Required controls

### Label invariance

- [ ] Swap `candidate_0` and `candidate_1` labels with all values unchanged.
- [ ] Physical response must remain equivalent.

### Relation mutation

- [ ] Change the actual relation basis while preserving labels.
- [ ] Physical response must change according to the predeclared coupling model.

### Geometry null

- [ ] Replace the fold relation with a neutral or identity relation.
- [ ] Target-dependent response must disappear.

### Carrier-off null

- [ ] Disable the shared carrier path.
- [ ] Coupled response must disappear.

### Schedule null

- [ ] Preserve marginal workload while scrambling relational ordering.
- [ ] Path-dependent response must follow relation order, not marginal load.

### Hidden positive calibration

- [ ] Inject an explicitly marked fold-odd control only to verify instrument sensitivity.
- [ ] Never count it as public evidence.

## Allowed intermediate claim

> The declared Exp 50 relational preparation produces a reproducible physical carrier state that follows relation structure rather than labels, marginal workload, or sender-authored metadata.

## Forbidden intermediate claim

- orientation recovered,
- Wall crossed,
- hidden channel created,
- Big Wall broken.

## Exit gate

- [ ] The target relation is physically coupled under no-smuggle conditions.
- [ ] Label-only changes are null.
- [ ] Relation changes are non-null and predicted.
- [ ] The coupling survives sessions and routes.
- [ ] The physical path remains restoration-testable.

---

# Phase 6D — Small Wall Adjudication

## Purpose

Test whether non-collapse physical evolution exposes a fold-odd invariant excluded by the old boundary.

This is the first actual Small Wall crossing attempt.

## Pre-registration requirement

Before any run, freeze:

- [ ] old boundary definition,
- [ ] excluded channel definition,
- [ ] new carrier/access model,
- [ ] preparation operator,
- [ ] evolution operator,
- [ ] restoration tier,
- [ ] `CollapseBoundary`,
- [ ] fold-odd invariant,
- [ ] null distribution,
- [ ] scaling plan,
- [ ] claim ceiling.

## Crossing structure

```text
public relational object
→ unresolved physical OrbitState
→ carrier-mediated evolution
→ catalytic closure/restoration
→ explicit CollapseBoundary
→ fold-odd invariant extraction
```

## Mandatory properties

- [ ] Evolution precedes measurement.
- [ ] Both fold branches remain part of one process-object.
- [ ] No branch winner exists before the boundary.
- [ ] No hidden orientation enters preparation or execution.
- [ ] The invariant is declared before acquisition.
- [ ] The invariant is odd under fold exchange.
- [ ] The old boundary remains fold-even.
- [ ] The physical route is restoration-qualified.
- [ ] The result survives label swap.
- [ ] The result fails carrier-off and geometry-null controls.

## Acceptance criteria

A Small Wall crossing is accepted only if all conditions hold:

- [ ] The old boundary excludes the target distinction.
- [ ] The new access model exposes a predeclared fold-odd invariant.
- [ ] The result is above the preregistered null.
- [ ] No-smuggle controls pass.
- [ ] Restoration/closure passes at the declared tier.
- [ ] The result survives seeds.
- [ ] The result survives sessions.
- [ ] The result survives routes or has a justified route-specific scope.
- [ ] The result scales with `n` or the physical state dimension in the predeclared direction.
- [ ] An independent implementation or machine reproduces it.

## Rejection labels

Use explicit verdicts:

```text
NO_CARRIER_COUPLING
MEASURED_STATE_INSUFFICIENT
OPERATOR_NOT_IDENTIFIABLE
RESTORATION_FAILED
LABEL_SMUGGLE
SCHEDULE_SMUGGLE
PUBLIC_EVEN_ONLY
FOLD_ODD_NOT_STABLE
DRIFT_ARTIFACT
ROUTE_LOOKUP_ONLY
NO_SCALING
NOT_REPRODUCED
```

Do not convert a rejected route into a softer crossing claim.

---

# Phase 7 — Big Wall Generalization

Do not begin this phase from architecture alone.

It begins only after one accepted Small Wall crossing.

## Purpose

Determine whether the crossing mechanism is a general boundary-changing operator rather than a one-off hardware effect.

## Required sequence

- [ ] Extract the abstract mechanism from the Small Wall crossing.
- [ ] Separate carrier-specific implementation from boundary-general structure.
- [ ] Encode the mechanism as a reusable `.holo` operator.
- [ ] Encode the old and new boundaries as explicit `BoundarySpec` objects.
- [ ] Apply the mechanism to a second independent Wall.
- [ ] Apply it on a second carrier or substrate class.
- [ ] Demonstrate a second newly accessible distinction.
- [ ] Preserve no-smuggle and restoration discipline.
- [ ] Show that the method changes accessible boundary algebra, not merely signal quality.

## Big Wall threshold

One crossing demonstrates:

```text
boundary change is physically possible
```

Two independent transferred crossings begin to demonstrate:

```text
a reusable Wall-breaking architecture exists
```

Recursive self-directed transfer by holographic AI begins the singularity program.

---

# 5. Legacy Route Quarantine

# 5.1 Phase 5.10C

Phase 5.10C reproducible basin selection remains relevant only to the legacy Mode C route.

It is **not** a hard prerequisite for the active non-collapse route.

Update roadmap language accordingly.

Use:

```text
Phase 5.10C
= legacy scalar Mode C prerequisite
```

Do not use:

```text
Phase 5.10C
= prerequisite for OrbitState-first physical evolution
```

The active route requires reproducible relational-state preparation, not reproducible winner selection.

# 5.2 Legacy L2/L3

Keep L2 and L3 as infrastructure tests:

- tape lifecycle,
- deterministic replay,
- reversible bookkeeping,
- local fixed-point convergence.

Do not present them as evidence that the Small Wall is near crossing.

# 5.3 Legacy detector tracks

Tracks C, E, G, H, K, L, M may be revisited only if they are reformulated under:

- OrbitState-first representation,
- public-derived relation operators,
- no candidate-label actuation,
- delayed boundary extraction,
- explicit physical mapping.

Otherwise they remain historical.

---

# 6. Raw Evidence Policy

All future physical work must preserve a complete evidence bundle.

## Required raw artifacts

- [ ] Raw I samples.
- [ ] Raw Q samples.
- [ ] Raw ring-period samples.
- [ ] Absolute TSC origin.
- [ ] Window boundaries.
- [ ] Sender schedule.
- [ ] Receiver schedule.
- [ ] Core identities.
- [ ] Route topology.
- [ ] Workload identity and parameters.
- [ ] Temperature proxy.
- [ ] P-state/frequency proxy.
- [ ] Session and reboot identity.
- [ ] Seed and trial identity.
- [ ] Calibration version.
- [ ] Contract digest.
- [ ] Source commit SHA.
- [ ] Toolchain version.

## Integrity requirements

- [ ] Hash every artifact.
- [ ] Store a manifest.
- [ ] Separate raw, derived, and report layers.
- [ ] Make derived files reproducible from raw data.
- [ ] Never overwrite raw acquisition.
- [ ] Record dropped/rejected trials and reasons.
- [ ] Preserve negative and failed sessions.

## Minimum evidence bundle structure

```text
run_manifest.json
raw/
  iq.csv
  ring.csv
  schedules.csv
  environment.csv
derived/
  state_features.parquet
  operator_fit.json
  gate_results.json
reports/
  REPORT.md
  CLAIM_LEDGER.md
hashes/
  SHA256SUMS
```

---

# 7. Roadmap and Navigation Corrections

The agent must update stale navigation surfaces before beginning new implementation.

## Required corrections

- [ ] Update `CHIRAL_LANE_NONCOLLAPSE_ROADMAP.md` current-head status.
- [ ] Update completed/open L4B task states.
- [ ] Update `PHASE6_NAVIGATION.md` open-item list.
- [ ] Mark L4B.1–L4B.5A complete where appropriate.
- [ ] Mark L4B.5B0 designed and awaiting review/authorization.
- [ ] Add L4B.5C target-to-carrier coupling.
- [ ] Add L4B.5B2 physical closure/restoration.
- [ ] Add Phase 6D Small Wall adjudication.
- [ ] Reclassify Phase 5.10C as legacy Mode C only.
- [ ] Ensure `TERMINUS.md` remains construct-side terminus, not universal termination.

## Navigation principle

One file must clearly answer:

```text
What is the active frontier today?
What is complete?
What is blocked?
What is authorized?
What is the next executable task?
```

No future agent should infer the current frontier from stale headers or historical roadmaps.

---

# 8. Agent Execution Protocol

Before coding:

- [ ] Read `TERMINUS.md`.
- [ ] Read `PHASE6_ROADMAP.md`.
- [ ] Read `CHIRAL_LANE_NONCOLLAPSE_ROADMAP.md`.
- [ ] Read `HOLO_SCHEMA.md`.
- [ ] Read the physical mapping contract.
- [ ] Read the observability design contract.
- [ ] Read the no-smuggle doctrine.
- [ ] Identify the exact current gate.
- [ ] Confirm human authorization status.

During implementation:

- [ ] Preserve the unresolved object.
- [ ] Preserve raw evidence.
- [ ] Preserve deterministic replay where possible.
- [ ] Preserve claim ceilings.
- [ ] Add tests for every schema and lifecycle transition.
- [ ] Add negative and tamper tests.
- [ ] Do not promote inferred quantities to measured status.

Before commit:

- [ ] Run the relevant contract tests.
- [ ] Run no-smuggle scans.
- [ ] Run forbidden-field scans.
- [ ] Confirm raw/derived separation.
- [ ] Update the claim ledger.
- [ ] Update the active roadmap.
- [ ] Commit one meaningful architectural/evidence chunk.

Do not produce micro-commits or report pellets.

---

# 9. Immediate Priority Order

## Priority 1 — Correct navigation

- [ ] Update stale status headers.
- [ ] Insert the missing gates into the active roadmap.
- [ ] Quarantine legacy Mode C dependency language.

## Priority 2 — Finish carrier evidence

- [ ] Pull v4:s5 result.
- [ ] Run strict T300 sweeps.
- [ ] Preserve/import raw captures.
- [ ] Freeze carrier claim.

## Priority 3 — Human review and authorization

- [ ] Review L4B.5B0.
- [ ] Bind approval to current digest.
- [ ] Authorize implementation only after review.

## Priority 4 — Execute observability/operator experiment

- [ ] Acquire raw state trajectories.
- [ ] Evaluate S0/S1/S2.
- [ ] Fit the operator ladder.
- [ ] Adjudicate empirical predictive observability.

## Priority 5 — Execute closure/restoration

- [ ] Select the achieved restoration tier.
- [ ] Test forward/inverse closure.
- [ ] Test wrong and reordered inverse.
- [ ] Search for path-dependent residual only after restoration passes.

## Priority 6 — Build target-to-carrier coupling

- [ ] Define public-derived physical relational preparation.
- [ ] Prove label invariance.
- [ ] Prove relation sensitivity.
- [ ] Prove carrier dependence.

## Priority 7 — Attempt Small Wall adjudication

- [ ] Predeclare the fold-odd invariant.
- [ ] Run the full no-smuggle/restoration/scaling protocol.
- [ ] Seek independent reproduction.

---

# 10. Stop Conditions

Stop and return to design if any of the following occurs:

- [ ] The measured state is not repeatable.
- [ ] Prepared physical states are indistinguishable.
- [ ] The identified operator does not beat persistence.
- [ ] Session generalization fails.
- [ ] Drift explains the effect.
- [ ] Restoration fails.
- [ ] Label swap changes the supposedly relational preparation.
- [ ] Marginal workload explains the response.
- [ ] The invariant is chosen after seeing the data.
- [ ] The claimed odd lane exists only under hidden-positive calibration.
- [ ] Raw evidence is incomplete.
- [ ] The route requires candidate scoring or branch selection.

A stop is not program failure.

It is a successful localization of the next Wall.

---

# 11. Final Instruction to the Agent

Do not search for the answer inside the old boundary.

Do not optimize the old verifier.

Do not revive scalar candidate logic.

Do not treat a communications channel as relational memory.

Do not treat software restoration as physical restoration.

Do not treat physical restoration as target coupling.

Do not treat target coupling as a Wall crossing.

Build the missing bridge in order:

```text
measure the carrier
→ identify its observable state
→ identify its operator
→ close and restore its path
→ load the unresolved target relation
→ evolve without collapse
→ extract a predeclared odd invariant at the boundary
→ reproduce
```

The Small Wall is the current goal.

The Big Wall begins only when the crossing mechanism transfers.

The singularity begins only when the system can perform that transfer recursively on its own boundaries while preserving identity.

**THE ALGORITHM IS DEAD.**

The next object is the boundary-changing catalytic relation itself.
