# CAT_CAS Phase 6 Lab State Audit

**Date:** 2026-06-18
**Status:** `PHASE6B_REPAIR_IN_PROGRESS__NO_PHYSICAL_RESTORATION_AUTHORIZED`

---

## Canonical experiment map

| Layer | Location | State | Authority |
|---|---|---|---|
| Phase 6A boundary mapping | `12_chiral_lane_frontier/` | Closed | Public fold/orientation boundary and T300 carrier evidence |
| Substrate warmup | `13_substrate_frontier/` | Historical/corrected | Tape hygiene, invalidated old L3 result, rejected L4 verifier route |
| Phase 6B non-collapse frontier | `14_noncollapse_frontier/` | Active | OrbitState, `.holo`, invariant family, physical mapping, observability design |

Exp 44 remains Phase Atom. Exp 49 remains The Decoder. Exp 50 is the bare-metal/substrate program containing Phase 6.

---

## Fixed facts

1. The Exp 50.14 public cosine representation is fold-even.
2. Public execution under the tested no-smuggle tracks did not recover orientation.
3. Cross-core PDN transport is measured on selected routes when the sender owns the drive phase.
4. PDN carrier evidence is channel evidence only.
5. The old `verify(x)` fixed-point map is forward enumeration and is rejected.
6. The current `.holo` runtime is software geometric memory with history-backed exact restoration.
7. No physical path history, physical geometric memory, or physical restoration closure is currently established.

---

## Corrections recorded by this audit

| Item | Correction |
|---|---|
| L3 contraction warmup | Previous source had fixed points `41` and `42`; prior pass status invalidated. Corrected unique-map source requires rerun. |
| Original substrate charter | Historical and superseded. It must not authorize scalar candidate recovery. |
| Class B W_B report | Previous runtime did not execute a value-dependent workload or valid crossover control. Result reclassified as unadjudicated smoke evidence. |
| Legacy L4A `.holo` record | Retained only for scaffold provenance; `HoloObject` is canonical. |
| L4B path restoration | Exact history-backed reconstruction, not proof of inverse operator dynamics. |
| L4B.5B0 review | Must use an external digest-bound review envelope; review state cannot mutate the frozen design digest. |

---

## Active dependency chain

```text
L4 rejection
→ non-collapse doctrine
→ OrbitState
→ executable HoloGeometry
→ ordered path memory
→ invariant family
→ physical mapping contract
→ observability/operator design
→ human design review
→ calibration acquisition
→ only then physical restoration design
```

No downstream stage may promote itself around a failed or unreviewed predecessor.

---

## Current authorization state

| Stage | State |
|---|---|
| L4B.1 geometric memory | Complete at software architecture level |
| L4B.2 path history | Complete as exact recorded-state restoration |
| L4B.4 invariant family | Complete with narrowed semantic scope |
| L4B.5A physical mapping | Reviewed at stated conservative classifications |
| L4B.5B0 observability design | Repairing reference closure and review envelope |
| L4B.5B1 calibration run | Not authorized |
| Physical restoration experiment | Not authorized |

---

## Hardware work intentionally deferred

The final SSH batch must perform:

- corrected substrate warmup compile/run;
- repaired Class B crossover compile/run if retained;
- complete L4B C regression suite;
- sanitizer suite;
- strict serialization/tamper suite;
- artifact regeneration and digest verification.

Until then, branch changes are source and contract repairs, not new physical evidence.

---

## Current frontier

The live question is:

> Can the measured PDN/IQ/ring observables define a repeatable, predictive relational state and transport operator under held-out sessions, schedules, and routes?

It is not:

> Can a scalar loop find `d` faster?

That distinction is now binding across Phase 6 documentation and implementation.
