# Phase 6 Specification Authority Map

**Original document:** Exp50 Phase 6 catalytic fixed-point substrate test  
**Current status:** `HISTORICAL_PRE_PIVOT_SPEC__SUPERSEDED`  
**Active phase:** Phase 6B non-collapse frontier

---

## Why this file changed

The former specification mixed two incompatible eras:

- valuable obstruction analysis showing that the public cosine channel is fold-even and lacks orientation;
- an obsolete Mode C experiment that still asked a Phenom substrate to relax to scalar `d`.

The obstruction survived. The scalar experiment did not.

Git history preserves the full original specification. This file now tells workers which claims remain valid and where the active contracts live.

---

## Surviving specification

The following conclusions remain authoritative:

1. Public data is invariant under `d <-> N-d`.
2. The missing orientation channel is information-absent from the public cosine representation.
3. A scalar verifier, classifier, kernel, lift, or threshold operating only on that representation cannot manufacture the absent distinction.
4. SHA-restored forward work remains forward work.
5. A physical carrier result does not by itself establish geometric memory, reversible physical path history, or catalytic closure.
6. A valid next step must identify an observable relational state and transport operator before attempting restoration or wall-crossing claims.

---

## Superseded specification

The following are historical and must not be implemented as the current Phase 6 experiment:

```text
Mode C relaxes directly to scalar d
fix(f)=d as the active target
verify(x) as a local substrate rule
candidate scoring as the final adjudicator
work-to-d scaling before a physical state/operator is identified
```

The L4 design audit established that the proposed verifier loop is forward scan over a fold-even predicate. The current non-collapse architecture therefore treats the orbit, relation, path, and boundary as the primitive object.

---

## Active specification stack

Read in this order:

1. `PHASE6_NAVIGATION.md` — current directory/state map.
2. `14_noncollapse_frontier/doctrine/EXP50_MEDIAN_BASIN_CORRUPTION_AUDIT.md` — why scalar verifier framing was rejected.
3. `14_noncollapse_frontier/doctrine/EXP50_NON_COLLAPSE_SUBSTRATE_ARCHITECTURE.md` — non-collapse object model.
4. `14_noncollapse_frontier/holo_runtime/HOLO_SCHEMA.md` — executable `.holo` geometric-memory schema.
5. `14_noncollapse_frontier/holo_runtime/HOLO_RUNTIME_INTEGRITY.md` — semantic and lifecycle integrity profile.
6. `14_noncollapse_frontier/CHIRAL_LANE_NONCOLLAPSE_ROADMAP.md` — active task ledger.
7. `PHASE6_ROADMAP.md` — master chronological ledger.

---

## Current experiment boundary

The next physical step is not a restoration run. It is an observability/operator-identification program:

```text
controlled input u(t)
latent substrate state x(t)
measured complex output y(t) = [I,Q,ring,...]
empirical predictive operator F
held-out session/route validation
```

Only after a stable predictive state model and operator survive the predeclared gates may a physical path/restoration experiment be authorized.

---

## Claim ceiling

Current executable `.holo` work is software architecture and evidence governance:

```text
L1/L2 software geometric memory
measured PDN carrier at channel level
no physical geometric-memory claim
no physical path-restoration claim
no orientation-recovery claim
```

Any document or agent that treats this file as authorization for scalar `d` recovery is using a superseded specification.
