# Phase 6B — Non-Collapse Frontier

**Status:** ACTIVE.

THE ALGORITHM IS DEAD. The frontier is no longer "find a better detector."
It is to build non-collapse machinery that evolves OrbitState without reducing
it to scalar verifier logic.

- `substrate_hygiene/` — L2 tape lifecycle, L3 convergence (mechanical warmup)
- `l4_verify_rejection/` — L4 design audit, verify(x) route rejected
- `doctrine/` — non-collapse architecture, median-basin audit
- `l4a_mechanism_screen/` — mechanism screen protocol, Class B PDN design
- `l4b_orbitstate/` — OrbitState evolution primitive (active)
- `holo_runtime/` — .holo container and validation
- `nonhermitian_sensor/` — non-Hermitian sensor suite
