#!/bin/sh
set -e

# post-checkout runs: <old-ref> <new-ref> <flag>
if command -v python3 >/dev/null 2>&1; then
  PY=python3
elif command -v python >/dev/null 2>&1; then
  PY=python
else
  exit 0
fi

"$PY" CAPABILITY/TOOLS/cortex/cortex_refresh.py || true
