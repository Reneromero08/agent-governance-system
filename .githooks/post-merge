#!/bin/sh
set -e

# Prefer the canonical location; keep best-effort fallback for older layouts.
python3 CAPABILITY/TOOLS/cortex/cortex_refresh.py 2>/dev/null || python CAPABILITY/TOOLS/cortex/cortex_refresh.py 2>/dev/null || true
