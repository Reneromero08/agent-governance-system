[pytest]
testpaths = CAPABILITY/TESTBENCH LAW/CONTRACTS NAVIGATION/CORTEX/tests
norecursedirs = MEMORY INBOX .git .github .claude .venv .kilocode
pythonpath = .
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
